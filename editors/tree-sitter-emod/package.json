{
  "name": "tree-sitter-emod",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for EMOD DSL",
  "main": "bindings/node",
  "scripts": {
    "test": "tree-sitter generate && tree-sitter test",
    "generate": "tree-sitter generate",
    "build": "tree-sitter generate && node-gyp build"
  },
  "license": "MIT",
  "dependencies": {
    "nan": "^2.20.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.25.0"
  }
}
