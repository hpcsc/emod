[tools]
"cargo:tree-sitter-cli" = "0.26.9"
# Needed by the export tests that vet generated CUE against internal/cue/schema.cue.
# Without it those subtests skip, so the schema check never runs in CI.
cue = "0.14.1"
goreleaser = "2.15.4"
task = "3.51.1"
