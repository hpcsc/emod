# Learnings

Durable facts distilled from completed implementation runs. Read before starting work
in this repo; append only learnings that generalise beyond the task that surfaced them.

## CLI diagnostic tests must assert the distinguishing message text
- Type: recurring-finding
- Learning: `internal/cli/*_test.go` tests that assert only `err.Error()` contains the path, `":1:"`, a line number, or a count are vacuous — any unrelated line-1 diagnostic satisfies them. The established repo convention is to assert the tokens that identify *this* diagnostic (see `internal/cli/validate_test.go:253-258` asserting rule names plus symbol names, and `internal/parser/parser_test.go:53-80` asserting the message names both the declared and the supported version). New CLI leaves for a new error kind must assert the same distinguishing content as the parser-level test one layer down, not just position and count.
- Apply when: adding or reviewing a test in `internal/cli` (validate, fmt, export, diagram, schema) that exercises a new error or diagnostic path.

## Run repo tooling through `mise exec --`, not bare PATH
- Type: constraint
- Learning: `/Users/davidnguyen/Personal/Code/emod/mise.toml` pins `cargo:tree-sitter-cli = "0.26.9"`, but the user's global `~/.config/mise/config.toml` pins a separate `tree-sitter = "0.25.10"` that wins on PATH in a non-activated shell. The two CLIs emit different generated output (0.26.x drops the version suffix from the `parser.c` banner and rewrites `src/tree_sitter/array.h`; `grammar.json` and `node-types.json` are identical), so a bare `task test:grammar` can leave the tree looking clean while `mise exec -- task test:grammar` produces a ~111/72-line diff. CI (`jdx/mise-action@v4`) sees only the repo pin, so the repo-pinned resolution is the one that matters. CI has no dirty-tree gate, so this drift stays green and is only caught by checking `git diff --exit-code` yourself.
- Apply when: running any Taskfile target whose output is committed (notably `task test:grammar`), or verifying that generated files in a diff are reproducible.

## Generated tree-sitter `src/` stays gitignored
- Type: constraint
- Learning: `editors/tree-sitter-emod/.gitignore` ignores `src/` alongside `bindings/node/` and `node_modules/` — the repo does not track generated artefacts. A grammar change commits `grammar.js` and its corpus tests only. Do not un-ignore `src/` to satisfy a "regenerated output is committed" criterion: it would make every grammar change carry a ~3k-line diff whose bytes depend on which CLI generated it, and `task test:grammar` runs `tree-sitter generate` first so nothing depends on those files being tracked.
- Apply when: touching `editors/tree-sitter-emod/grammar.js`, or reviewing a diff that changes the grammar.

## A task criterion requiring "committed" output cannot close
- Type: constraint
- Learning: implement-flow commits a task only *after* it closes, so an acceptance criterion phrased as "... and the output is committed" is unsatisfiable from inside the task — the audit re-checks `git log`/`git ls-tree`, finds nothing, and reports the criterion unmet on every attempt until `maxResolve` is exhausted. This burned all 5 attempts on US-001 Task 4. Phrase the criterion against the working tree instead — "regenerating leaves the tracked files byte-identical", "the file exists and matches X". Note that "`git status` is clean" is *also* unusable: a task under review always has uncommitted work.
- Apply when: writing or reviewing acceptance criteria during decomposition, especially for tasks that produce generated or checked-in artefacts.

## New DSL keywords must stay usable as field names
- Type: pattern
- Learning: emod keywords are not reserved words — they remain valid identifiers inside `fields` blocks. The Go parser handles this with `Instance.checkIdentifierLike()` (`internal/parser/parser.go`), and the tree-sitter grammar mirrors it with a permissive `any_identifier: $ => /[a-zA-Z_][a-zA-Z0-9_]*/` plus a keyword token narrow enough to match only in its own position (e.g. `version_header` bakes the separator into the token via `token(seq('emod', /[ \t]+/))` with `token.immediate` digits, so a bare `emod` cannot pair with a number on the next line). Adding a keyword to `internal/lexer/token.go` therefore needs a corresponding "field named X" case on both sides — see `internal/parser/parser_test.go:224` "keywords are usable as field names" and `editors/tree-sitter-emod/test/corpus/version_header.txt::Field named emod inside a fields block`.
- Apply when: introducing a new keyword into the emod language, in the Go lexer/parser or the tree-sitter grammar.

## Formatter output always begins with `emod N`
- Type: convention
- Learning: `formatter.Format` unconditionally emits a version header as the first line (`emod %d` from `pinnedVersion`, preserving `Model.Version` when `VersionDeclared` is set, otherwise `ast.SupportedVersion`). Every expected string in `internal/formatter/formatter_test.go` and every canonical-output constant in `internal/cli/fmt_test.go` must start with that line, while *input* fixtures may omit the header (the parser implies version 1 and leaves `VersionDeclared` false). `fmt --check` fails a file whose only deviation is the missing header.
- Apply when: adding a formatter golden, a `RunFmt` fixture, or an `.emod` round-trip test.

## `docs/dsl-reference.md` anchors embed the section number
- Type: convention
- Learning: every heading in `docs/dsl-reference.md` is `## <n>. <Title>`, so its in-document links are number-prefixed slugs (`[Slice Patterns](#6-slice-patterns)`, `[Flows](#7-flows)`, `[Cross-References](#10-cross-references)`). Inserting, removing or reordering a section renumbers every heading below it *and* invalidates each link that cites one of those numbers — four such links exist today. Nothing catches the drift: neither `Taskfile.yml` nor `.github/workflows/ci.yml` runs a markdown link check, and a stale `#7-cross-references` link (pointing at "7. Fields") survived an earlier insertion unnoticed. After editing the section list, re-derive the anchors by listing `^## [0-9]+\.` and `\(#[0-9]+-` and reconciling the two.
- Apply when: adding, removing or reordering a numbered section in `docs/dsl-reference.md` (or any doc using `## <n>. Title` headings).

## Keyword surfaces fan out past the lexer, parser and tree-sitter grammar
- Type: pattern
- Learning: a new emod keyword touches several hand-maintained lists that no test cross-checks against `internal/lexer/token.go`: the TextMate alternation at `editors/vscode/syntaxes/emod.tmLanguage.json:63`, `keywordDescriptions` in `internal/lsp/hover.go:13`, and the per-context lists in `internal/lsp/completer.go`. `isKeyword` (`internal/lsp/hover.go:37`) is an *ordinal range* — `k >= lexer.KeywordModel && k <= lexer.KeywordExternal` — so every `Kind` appended after `KeywordExternal` (`mode`, `tags`, `decides_on`, `where`, `and`, `or`, `not`, `tag`, `events`, `emod`) is silently invisible to hover no matter what the description map contains. Decide per keyword whether each surface should cover it; do not assume adding the token propagates.
- Apply when: adding a keyword to `internal/lexer/token.go`, or auditing why a valid keyword has no VS Code highlighting, hover text or completion.

## Never write emod source with `%q` — the language has no escape sequences
- Type: constraint
- Learning: an emod string literal runs verbatim from one quote to the next; the lexer performs no unescaping. `%q`/`strconv.Quote` therefore breaks formatter idempotence — a backslash or tab in the text is written as two characters that read back as themselves, so `C:\hotel` becomes `C:\\hotel` then `C:\\\\hotel` on successive `emod fmt` runs. `formatter.go` has `quoted(text)` (`internal/formatter/formatter.go:47`) for this; every name and every description goes through it. The counterpart obligation is a round-trip subtest per hazard character (backslash, tab, quote, `%`, non-ASCII) asserting a second `Format` of the output produces identical bytes — `internal/formatter/formatter_test.go` has that table, and note the JSON/CUE exporters do the opposite (they must escape, and have their own export-then-decode subtests pinning that the text survives).
- Apply when: emitting emod source text from Go — the formatter, any codegen, LSP edits, or example generation.

## Additive output changes owe a byte-identical receipt for models that do not use the feature
- Type: convention
- Learning: every writer in this repo (`internal/formatter`, `internal/export`, `internal/diagram/drawio.go`, `internal/diagram/svg.go`) is expected to produce the exact bytes it produced before when the new field is absent, and the change must prove it rather than assert it. The diagram packages do this with `withoutDescriptions` (`internal/diagram/contract_test.go:589`), which strips the new prose from a model and compares shapes, fills, positions and arrows against the described render, so a change that added, moved or repainted a box fails. The undescribed `test.HotelReservation` fixture exists to be the same witness for the parser and CLI layers. State the receipt in the commit message; a reviewer reads its absence as unverified scope creep.
- Apply when: adding a field to any AST node that a formatter, exporter or diagram renderer writes out.

## A new exported field must land in JSON, CUE and `schema.cue` in the same change
- Type: constraint
- Learning: two subtests couple the three surfaces. "CUE and JSON exports describe the same model" (`internal/export/export_test.go:3317`) decodes both exports of one fixture and `require.Equal`s the documents, so teaching one format about a field fails immediately; "output conforms to the schema's Model definition" runs `cue vet -d '#Model'` against `internal/cue/schema.cue`, so the embedded schema must gain the field too. The diagram JSON document is deliberately *not* coupled: `jsonDiagramEvent` (`internal/export/diagram.go`) forks `jsonEvent` precisely so a new AST field cannot leak through a translation's inline event into the node-and-edge contract, and a subtest walks the whole diagram document asserting the key appears nowhere. Do not re-merge those two structs.
- Apply when: adding a field to the JSON or CUE export, or wondering why the diagram document has its own event type.

## The tree-sitter grammar must never be stricter than the Go parser
- Type: constraint
- Learning: `editors/tree-sitter-emod/grammar.js` is a deliberately looser mirror — a grammar stricter than the language red-squiggles files `emod validate` accepts, which is the one failure direction that matters. Every block body in `internal/parser/parser.go` is an unbounded `for !p.check(lexer.CloseBrace)` loop with no arity restriction, so `command X { fields {…} fields {…} }` is valid emod (verified: `go run ./cmd/emod validate` exits 0). Constructs therefore use `buildDescribedBlock($, …)` — `'{' repeat(choice($.description, ...items)) '}'` — and an `optional($.rule)` in a block body is a bug, not a constraint. Re-imposing at-most-one would also need an ambiguous `repeat($.description) optional(...) repeat($.description)`.
- Apply when: editing `grammar.js`, or reviewing a grammar diff that appears to "widen" a rule from `optional` to `repeat`.

## Shared fixtures come in an unfeatured/featured pair, guarded by a walk that must be extended
- Type: pattern
- Learning: `internal/test/fixtures.go` holds `HotelReservation` (uses no optional feature, witnesses that plain files still parse and still render byte-identically) alongside `DescribedHotelReservation` (exercises the feature everywhere it can appear, and is the one model the formatter, export and diagram packages assert against). Keep both roles when adding the next optional feature. The guard subtest "the shared described model describes every construct that accepts one" (`internal/parser/parser_test.go:3157`) only guards what `describableConstructs` (`:4050`) actually visits — the model, its actors, then every context and the slices below it — and what its `require.Subset` list names. A construct that gains the field but is not added to both is silently unguarded: dropping its description leaves the test green while the downstream round-trip tests quietly lose that input. Verify an extension by deleting the description from the fixture and confirming the test fails.
- Apply when: adding to or editing `internal/test/fixtures.go`, or extending an optional field to a new construct.

## De-duplicate before a fan-out edit, and land the de-duplication with proof
- Type: convention
- Learning: when the same edit would be copied into N sibling writers, this repo extracts the shape first: `writeCUEList`/`lineIfSet` replaced twelve `write*List` methods, `sliceXPositions`/`contextBounds`/`layoutWidth` replaced four copies of the layout arithmetic, `svgRectAttributes`/`svgRectElement` replaced three rect builders, `blankLineBetweenBlocks` replaced the `needsBlank`/index pairs, and `buildDescribedBlock` replaced nine brace-repeat-choice spellings. The extraction is welcome; what gets flagged is shipping ~300 mechanical lines inside a field-addition commit with nothing showing the output did not move. Carry a differential receipt (randomized-model diff, or the fixture across every format) and prefer a separate preparatory commit when the refactor is larger than the feature edit. Also give extracted helpers the parameters their siblings take — a `write*` helper that hardcodes indent level 0 while every sibling takes `level` invites a nested call that silently emits at column 0.
- Apply when: an edit would otherwise be pasted into more than about three sibling functions.

## A new block entry keyword owes three things to the parser's diagnostics
- Type: pattern
- Learning: (1) every construct's `default:`/`else` branch spells out the entries it accepts — eight messages read `expected description, … in <construct>` — so a new entry means editing all of them, and the tests assert that text. (2) A keyword whose value is malformed must report once: recover by draining the rest of the line with `p.checkSameLineAs(keywordTok)` and stopping at `lexer.CloseBrace` (see `parseDescriptionInto`, `internal/parser/parser.go:1288`), not with a single `p.advance()` — one unquoted description otherwise emits five diagnostics as each remaining word fails to match a block entry. (3) Pin it with `require.Len(t, diags, 1)`; that assertion appears throughout `internal/parser/parser_test.go` and is what makes the cascade a test failure.
- Apply when: adding a keyword that may appear inside a block construct in `internal/parser/parser.go`.

## Ask the lexer which keywords exist; never restate the set and never range over `Kind`
- Type: convention
- Learning: `internal/lexer/token.go` exports `Keywords() []string` (the thirty spellings, sorted, from the `keywords` map) and `Kind.IsKeyword()` (a lookup in the `keywordNames` inversion). Every keyword-coverage test iterates `lexer.Keywords()` — `internal/lexer/tokenizer_test.go:14`, `internal/parser/parser_test.go:225` and `:243`, `internal/oracle/oracle_test.go:44` — so a keyword added to the map is covered on the day the lexer learns it. The two failure modes this replaced both went unnoticed for a long time: a hand-written list drifts (the tokenizer's named twenty-one of thirty, silently omitting `mode`, `tags`, `decides_on`, `where`, `and`, `or`, `not`, `tag`, `events`), and an ordinal range over the iota block silently excludes everything declared past its bound (`checkIdentifierLike` was `typ < lexer.Identifier`; `isKeyword` in `internal/lsp/hover.go:37` is still `k >= lexer.KeywordModel && k <= lexer.KeywordExternal` and should be `k.IsKeyword()`).
- Apply when: writing any code or test that must treat "every keyword" uniformly, or adding a keyword to `internal/lexer/token.go`.

## A line-oriented declaration must gate every optional trailing token on the first token's line
- Type: constraint
- Learning: block bodies in `internal/parser/parser.go` loop until `}` or EOF, so nothing but an explicit line check stops a declaration from reaching into the next line. `parseField` read name, then type, then an optional modifier guarded only by identifier-likeness, and a modifier-less field therefore ate the following line's name — `roomType string` above `guestId string required` parsed as one field `roomType string guestId` plus a bogus `string required`. The guard is `p.checkIdentifierLikeSameLineAs(nameTok)` (`checkSameLineAs` + `checkIdentifierLike`), positioned against the *first* token of the declaration, and the failure path drains with the shared `p.skipRestOfLine(tok)` — which also stops at `lexer.CloseBrace`, so `fields { guestId }` still closes its block. Formatter idempotence does not catch this class: `emod fmt` rewrote the file into the mangled shape and was then byte-stable on the wreckage. Only a parse→format→reparse comparison against the original model does (`internal/formatter/formatter_test.go:427` "round-trip through the parser").
- Apply when: adding or editing a construct in `internal/parser/parser.go` whose parts sit on one line with any part optional.

## Exercise an omitted optional part mid-block, never as the last entry
- Type: pattern
- Learning: an optional part left out at the end of a block has nothing to run into, so it witnesses nothing. Every field in `HotelReservation`, `DescribedHotelReservation` and every `.emod` under `examples/` carries a modifier, which is why a `parseField` that could swallow the next line survived every round-trip, alignment and golden test in the repo. `KeywordFieldSearchCatalog` (`internal/test/fixtures.go`) deliberately puts its modifier-less fields mid-block ahead of a further field, and that placement is the whole point of the fixture — a reviewer should read a moved-to-the-end instance as coverage lost.
- Apply when: writing a fixture, corpus case or golden that is meant to cover an optional element being absent.

## A differential receipt must first prove the twin actually differs
- Type: convention
- Learning: the strip-or-rename helper behind a differential ("the two renderings are identical apart from X") is itself untested, and one that quietly misses a nesting level — e.g. the event nested inside a `translation`, which `test.WithOrdinaryFieldNames` has to walk explicitly — turns the receipt vacuous: identical inputs trivially produce identical output. Both differentials added for keyword-named fields open with `require.NotEqual` on the part that must move (`internal/export/export_test.go:2832`, `internal/diagram/contract_test.go:203`) before asserting everything else is equal. Pair every `withoutX`/`withOrdinaryX` comparison with that guard.
- Apply when: writing a test that compares a model against a transformed twin to prove a change reached exactly one surface.

## Name an extracted helper after the contract its callers rely on
- Type: recurring-finding
- Learning: two advisories in one story landed on the same defect — a helper whose name describes the happy path while callers depend on something else it does. `skipRestOfLine` (`internal/parser/parser.go`) also halts at `lexer.CloseBrace`, which is exactly what lets `fields { guestId }` close its block, so the name understates the stop condition (`skipRestOfLineOrBlockEnd` states it). `test.WithOrdinaryFieldNames` (`internal/test/fixtures.go`) uses the Go `With…` prefix, which promises a modified copy, but renames in place and returns the same pointer, so a caller keeping a reference to the original silently loses the keyword names (`RenameFieldsToOrdinary`, or a deep copy, removes the trap). Before naming an extraction, state the postcondition a second caller would rely on, then check the name says it.
- Apply when: extracting a shared helper from duplicated code, or adding an exported helper to `internal/test`.

## Put a new parser subtest in the group that owns the construct
- Type: convention
- Learning: `internal/parser/parser_test.go` is one umbrella split into thirteen top-level `t.Run` groups named for the area under test — "version header" (:23), "model and actors" (:322), "contexts, aggregates and slices" (:568), "commands, events and flows" (:764), "triggers" (:987), "views" (:1124), "automations" (:1392), "translations" (:1622), "error reporting" (:1849), "event sources and tags" (:2251), "descriptions" (:2587), "comments" (:3295), "decides_on" (:3491). A subtest belongs to the group that owns the construct it exercises, not to whichever group happened to hold the nearest prior example: the keyword-as-field-name subtests sit in "version header" (:225, :243) because a lone `emod`-named-field case lived there, though field parsing belongs under "commands, events and flows" and construct-name rejection under "error reporting".
- Apply when: adding a subtest to `internal/parser/parser_test.go` (or any of this repo's large umbrella test files).

## A declared flag works after the file argument; an undeclared one fails in either position
- Type: constraint
- Learning: `github.com/urfave/cli/v2 v2.27.7` reorders arguments, so a flag the command declares is honoured wherever it is written. Verified at `5c5c647`: `emod diagram reservation.emod --format mermaid` prints mermaid and writes no file, `emod diagram reservation.emod --specs` writes a diagram carrying its spec cards, and `emod export reservation.emod --format cue` prints CUE while `--format json` prints JSON. What fails is an *undeclared* flag: only `glossary` declares `Aliases: []string{"f"}` (`internal/cli/app.go`), so `-f` exits 1 with "flag provided but not defined: -f" on `diagram`, `validate`, `lint`, `export` and `slices list` — in **both** positions, which is what shows the cause is the missing alias and not the placement. This entry previously claimed the opposite, having generalised from `-f` observations to every flag; the version has never moved, so the claim was wrong when written rather than stale, and it cost US-016 a decided open question ("`--specs` is written before the file argument") resting on a constraint that does not exist. Its other half is stale too: `glossaryPathAndFormat` no longer exists, and `RunGlossary` takes its path and format directly.
- Apply when: writing an acceptance criterion or a README example that puts a flag after the file, or debugging a flag that appears to be ignored — check first whether the command declares the spelling you used.

## New file-taking CLI commands compose `parseModelFile` and `reportExitError`
- Type: convention
- Learning: `parseModelFile(command, path)` (`internal/cli/parse.go`) is the shared read → `lexer.Scan` → `parser.Parse` → `formatText(diagnostics)` opening, and it produces the missing-file `LintError` — message `"<command> requires exactly one file argument"`, `ExitCode: 1`, `Cause: ErrMissingFileArgument` — that CLI tests branch on with `errors.As`. `validate.go:25`, `export.go:33`, `lint.go:117` and `diagram.go:35` each still hand-roll that same pair; `slices` and `glossary` call the helper. On the app side every `Action` is one line, `return reportExitError(RunX(...))` (`internal/cli/app.go:12`), which unwraps `*LintError` onto `urfave.Exit("", code)` and prints the message to stderr. Compose both rather than copying an older command's body, or the new command drifts on the cause callers match and on how diagnostics reach stderr.
- Apply when: adding a command to `internal/cli` that takes an `.emod` file.

## AST-to-bytes renderers are named `Export<Format>`
- Type: convention
- Learning: every renderer that turns a model into bytes takes the `Export<Format>(model *ast.Model) ([]byte, error)` form — `diagram.ExportMermaid`, `ExportASCII`, `ExportDrawio`, `ExportSVG` (the diagram four take a trailing `Style`), `export.ExportJSON`, `export.ExportCUE`, `export.ExportDiagramJSON`. `internal/glossary` is the single deviation (`RenderMarkdown`, `RenderJSON`), which a reviewer flagged; it stands as written. A third verb (`Write…`, `Generate…`, `Print…`) fragments the family further, so a new renderer package takes `Export<Format>` and a new format inside `internal/glossary` matches its in-package siblings.
- Apply when: adding an output format anywhere in the repo, or naming a new package's entry point.

## An assertion whose expected value comes from the code under test is the recurring review finding
- Type: recurring-finding
- Learning: three stories running, the reviewer's surviving advisory has been an assertion that cannot fail — a CLI diagnostic test asserting only position and count, a differential comparing a twin that was never proved to differ, and now `require.Equal(t, string(rendered), string(again))` in `internal/glossary/glossary_test.go` deriving its expected value from a second call to the renderer. A repeat-call assertion only has teeth where the path ranges over a Go map: `internal/glossary` never does (`actorDescriptions` and the `seen`/`referenced` maps are lookup-only), `internal/diagram/mermaid.go:158` sorts its tag keys precisely because it does. Before writing an assertion, name an input that makes it fail; if the only such input needs the implementation changed, it is filler — and filler bolted onto a subtest named for something else (declaration order, here) also gives that subtest a second reason to fail.
- Apply when: adding a "render/format it twice" or "compare against a re-derived value" assertion, or reviewing a subtest that asserts more than the behaviour its name states.

## `internal/glossary` collects once and renders twice; the collected document is the JSON shape
- Type: pattern
- Learning: `newDocument(model)` (`internal/glossary/glossary.go`) is the only walk of the AST — `RenderMarkdown` (`markdown.go`) and `RenderJSON` (`json.go`) both start from it — and `document`/`contextSection`/`term` carry the JSON tags directly, so there is no second serialized type to keep in step. Markdown-only concerns live in `markdown.go`: `termGroup`, the heading constants and the heading levels. Two rules the shape encodes: `description` deliberately takes no `omitempty` while every collection does, because an undescribed construct must arrive with an empty string (the JSON counterpart of the heading markdown keeps) — this is the opposite of `internal/export`, whose documents `omitempty` their descriptions, so its structs are the wrong thing to copy here; and a new term kind (US-005's invariants, say) is a field on `contextSection` plus one more `termGroup` passed to `appendGroups`, not a new traversal. `slice`, `trigger`, `automation` and `translation` contribute no term of their own — a `translation`'s nested event is listed among the events — and `docs/dsl-reference.md` section 10 states that.
- Apply when: adding a term kind, an output format, or a field to the glossary, or wiring a new JSON document elsewhere in the repo.

## A second `require.Contains` on one message is often shadowed by the first
- Type: recurring-finding
- Learning: asserting that a diagnostic names both a symbol and its scope with two `require.Contains` calls silently collapses when the scope name is a substring of the symbol — `require.Contains(t, diags[0].Message, "Loan")` at `internal/validator/validator_test.go:1247` cannot fail, because `"OneCopyPerLoan"` asserted on the line above already contains `Loan`, and mutating the message to drop the scope entirely leaves the subtest green. Assert the qualified fragment the message actually spells (`aggregate "Loan"`), or follow the group's own sibling at `:1408` and `require.Equal` the whole formatted line (`shelf.emod:12: invariant "Zulu" is already declared in aggregate "Loan"`), which is the shape the ordering subtests use. Before adding a `Contains` to a message that already has one, check the new needle is not inside the old one.
- Apply when: writing or reviewing a test that asserts a diagnostic message names more than one thing (symbol plus scope, rule plus name, type plus field).

## `emod fmt` canonicalises order, so a fmt golden is never the input re-indented
- Type: constraint
- Learning: `formatter.writeSlice` (`internal/formatter/formatter.go:145`) emits a slice's entries in a fixed kind order — description, trigger, commands, events, views, automations, translations, flows, specs last — and `writeContext`/`writeAggregate` hoist every `invariant` to just after `description`, above the first nested block. The AST stores each kind in its own slice, so source interleaving is unrecoverable and formatting moves declarations: none of `test.HotelReservation`, `test.KeywordFieldSearchCatalog` or `test.InvariantLibraryLending` is byte-stable under `Format` (verified — flows migrate past translations, field columns re-align). The consequence for `internal/cli/fmt_test.go`: pin a canonical `<name>FormattedEmod` constant (`:118`, `:135`) and pass *that* to `requireFmtSettlesOn` (`:381`). Passing `readFile(t, path)` back as the expected value, or `require.Contains`-ing lines that appear verbatim in the input fixture, leaves a subtest that also passes when `RunFmt` writes the file back unchanged — flagged twice by independent reviewers on one task (`:323`).
- Apply when: adding a subtest to `internal/cli/fmt_test.go`, writing a formatter golden, or choosing an input fixture for a formatting test.

## JSON and CUE order their document keys differently — do not mirror one struct into the other
- Type: convention
- Learning: in `internal/export/json.go` every `json*` document type opens with `Name`, then `Description`, then the positions, then `Comments`, then its children; the CUE side (`cueWriter.write*`, `internal/export/cue.go`, `internal/cue/schema.cue`) opens with `comments?` and then `name`. `jsonInvariant` (`json.go:65`) shipped with `Comments` ahead of `Name`/`Statement` — the CUE order transplanted into the Go struct — so the emitted invariant object reads with its keys in a different order from every sibling. Nothing catches this: the "CUE and JSON exports describe the same model" subtest decodes both into Go values before comparing, and key order does not survive decoding. Copy the ordering from a `json*` sibling, not from the schema.
- Apply when: adding a document type or field to `internal/export/json.go`, `internal/export/cue.go` or `internal/cue/schema.cue`.

## A new shared fixture owes `internal/oracle` a zero-diagnostic subtest
- Type: convention
- Learning: `TestCheck`/"clean input" (`internal/oracle/oracle_test.go:24`) holds one `require.Empty(t, oracle.Check(...))` subtest per fixture in `internal/test/fixtures.go` — `HotelReservation`, `DescribedHotelReservation`, `KeywordFieldSearchCatalog`, `InvariantLibraryLending` — because `oracle.Check` runs lexer, parser, validator *and* linter, so the fixture is pinned as something `emod validate` and `emod lint` both accept. A fixture that trips a lint rule breaks every downstream CLI test asserting exit 0, and DCB shapes are the usual tripwire: a `mode dcb` context needs tags on its events and a `decides_on` reaching them, or `dcb/untagged-event` and `dcb/orphan-tag-key` fire. Write the fixture, add the oracle subtest, and let it tell you the model is not clean.
- Apply when: adding a constant to `internal/test/fixtures.go`, or writing a `mode dcb` model anywhere in tests or `examples/`.

## A new block entry goes after `description` and ahead of nested blocks, in every writer
- Type: convention
- Learning: an entry added to a `context`/`aggregate` body lands in seven places, all of which agree on placement — `internal/formatter` (`writeContext:113`, `writeAggregate:131`, right after `writeDescription`, before the slice/aggregate loop), the `json*` struct fields, `cueWriter` (`export.go:1247`, `:1255`), `internal/cue/schema.cue`, `internal/glossary` (the `contextSection` field and `appendContext`'s `appendGroups` call, both before the aggregate loop), the tree-sitter entry list, and `docs/dsl-reference.md`. The writer that hurts to forget is the formatter: it renders from the AST and emits only what it knows, so `emod fmt` silently *deletes* an entry it has never heard of, and neither idempotence nor an existing golden notices — the guard is a parse → format → reparse comparison against the original model, plus the featured fixture that carries the entry.
- Apply when: adding a declaration entry to a block construct in `internal/parser/parser.go`.

## The two export guards cannot see a list neither writer emits
- Type: constraint
- Learning: `jsonContext` (`internal/export/json.go`) listed `invariants` and `aggregates` but no `slices`, and `cueWriter.writeContext` matched it, so every slice a `mode dcb` context declares directly on itself was missing from both model exports while the diagram document — which walks `ctx.Slices` — carried all four of `examples/dcb_model.emod`. Neither guard reaches that class of gap: "CUE and JSON exports describe the same model" compares the two exports against each other, so a list neither writer emits agrees trivially, and `cue vet -d '#Model'` constrains only the keys a document actually has, `#Context` declaring no `slices?` key to reject it with. What does catch it is a transcribed-map subtest — `listsKeyedBy(doc, "slices", "name", …)` over a fixture declaring the construct in every position the grammar allows, read back against the names an author wrote. With the key on all three surfaces a DCB model's JSON and CUE exports carry a `slices` list under each context, while the CLI listing printed "No slices found." for the same file, walking only `ctx.Aggregates` until `c36cb90` put every walk onto the `ast` traversal helpers.
- Apply when: adding a field or a list to `internal/export/json.go`, `internal/export/cue.go` or `internal/cue/schema.cue`, or relying on the export-parity and schema-conformance subtests to cover a construct.

## `RuleName` marks a diagnostic `emod lint --explain` can describe
- Type: constraint
- Learning: `diagnostic.Entry.RuleName` is what prints the `[rule]` bracket (`internal/diagnostic/entry.go:34`), and `emod lint --explain <name>` resolves it against `ruleDescriptions` in `internal/linter/descriptions.go` — fourteen entries, none of which is a validator rule. Hard errors that no configuration can silence therefore carry no `RuleName`: the cross-reference checks and the invariant redeclaration in `internal/validator/validator.go` set only position, severity and message. The two exceptions are live bugs, not precedent — `orphanDiagnostics` (`validator.go:146`) stamps `orphan-command`/`orphan-event`, so `emod validate` prints `[orphan-command]` while `emod lint --explain orphan-command` answers `unknown rule "orphan-command"` and exits 1 (verified). Naming a new rule obliges you to register its description; if there is nothing to configure, leave the field empty.
- Apply when: adding a diagnostic to `internal/validator` or a rule to `internal/linter`.

## A slice has two homes, and much of the repo still walks only one
- Type: recurring-finding
- Learning: a slice hangs off an aggregate (`agg.Slices`) *or* directly off a `mode dcb` context (`ctx.Slices`), so every walk needs both branches. Three places get it right and are the shapes to copy: `newModelIndex` (`internal/validator/validator.go:65-70`), `contextSlices` (`internal/glossary/glossary.go:63-66`) and `declaredSlices` (`internal/test/fixtures.go`). The ones that walked aggregates only were the CLI listing and every LSP walker — `definition.go`, `hover.go`, `references.go`, `semantictokens.go` — leaving go-to-definition, hover, find-references and highlighting blind to a DCB context's own commands, events and views, until `c36cb90` moved them onto the helpers; `emod slices list examples/dcb_model.emod` lists them now (verified). `jsonContext`/`cueWriter.writeContext` were the same until `slices` was added to both plus `#Context` in `internal/cue/schema.cue`. A fixture that declares the construct in only one home cannot catch any of this, which is why `test.SpecLibraryLending` declares specs in both and `test.SpecLibraryLendingSpecNames` transcribes the two homes' names as one list.
- Apply when: writing any walk over slices (a validator rule, an exporter, a CLI listing, an LSP feature), or building a fixture for a construct that lives on a slice.

## A bracketed list's terminator set decides whether one typo cascades
- Type: constraint
- Learning: `parseIdentifiersUntil(atEnd, msg)` (`internal/parser/parser.go:748`) errors and advances on every token that is not an identifier, so its `atEnd` predicate is the whole recovery story. The spec lists pass `atSpecEventListEnd` (`:547`), which stops at `]`, `}` and the sibling entry keywords `given`/`when`/`then`, and an unclosed `given [CopyBorrowed` reports exactly one diagnostic. `parseIdentifierList` (`:718`, serving `events` and `subscribes`) passes only `p.check(CloseBracket) || p.isAtEnd()`, and an unclosed `subscribes [CopyBorrowed` swallows the rest of the file: 16 diagnostics, including `event "string" does not exist` and `event "required" does not exist` from the following `fields` block (verified). Existing tests miss it because they only close their brackets. Give a new list the block-end and sibling-keyword stops, and pin it with `require.Len(t, diags, 1)` on the *unclosed* input.
- Apply when: adding a bracketed identifier list to `internal/parser/parser.go`, or reviewing a change to `parseIdentifiersUntil`'s callers.

## Diagnostics gathered from more than one AST collection must be position-sorted
- Type: convention
- Learning: the AST keeps each kind in a field of its own, so field order is not source order: a context's invariants sit apart from its aggregates', and a spec's `Given`, `When` and `Then` are three fields the author may have written in any order (`test.SpecLibraryLending` deliberately writes one spec's `then` above its `when`). Both new checks therefore sort by `comparePositions` before emitting — `scopedInvariantDiagnostics` (`internal/validator/validator.go:281`) and `specDiagnostics` (`:339`) — and acceptance criteria in this repo ask for "declaration order" explicitly. The assertion that pins it is one `require.Equal` against `reportedLines(diags)` (`internal/validator/validator_test.go:2908`), which shows the whole list on failure; `require.Len` plus `diags[0].String()` is the dominant older spelling (34 sites) and cannot catch a misordering at all.
- Apply when: adding a validator check that collects findings from more than one AST field or scope, or asserting on a multi-diagnostic result.

## `ast.ThenClause` dispatches through five type switches, none of which errors
- Type: pattern
- Learning: `Spec.Then` is a sealed interface (`internal/ast/ast.go:111`, marker method `thenNode()`) with `ThenEvents` and `ThenRejected` today. Adding a variant means editing five sites, and every one of them fails silently on a kind it has not heard of: `parseSpecOutcome` (`internal/parser/parser.go:498`), `formatOutcome` (`internal/formatter/formatter.go:390`, `default: return ""` — so `emod fmt` deletes the whole `then` line), `convertSpecOutcome` (`internal/export/json.go:437`, returns nil — JSON omits `then`), `cueWriter.writeSpecOutcome` (`internal/export/cue.go:144`), and the two bare type assertions in the validator (`validator.go:249` for `ThenRejected`, `:344` for `ThenEvents`). There is no `.golangci.yml` in the repo, so no exhaustiveness check backs any of it. `docs/proposals/specs-and-metadata-proposal.md:358` plans `ThenView` and `ThenCommand`, so this is the next story's trap: add the variant, and the parse→format→reparse comparison against the original model is the only thing that will notice the formatter dropped it.
- Apply when: adding a variant to `ThenClause` (or to `ast.PredicateExpr`, which fans out the same way through `internal/validator`, `internal/linter` and `internal/formatter`).

## A spec is not a reference: a command only a spec exercises is still orphaned
- Type: constraint
- Learning: `index.referencedCommands` and `index.producedEvents` are filled from flows, triggers, automations, translations and external sources — not from specs. A slice holding `command BorrowCopy`, `event CopyBorrowed` and a spec naming both, with no `flow`, reports `[orphan-command]` and `[orphan-event]` (verified). So every spec-carrying fixture still needs its flow, which is what `test.SpecLibraryLending` does in all four command slices, and the `oracle.Check` zero-diagnostic subtest is what tells you if it does not. The same asymmetry is the thing to decide deliberately if a lint rule about spec coverage is ever added: today a spec proves nothing about a construct being reachable.
- Apply when: writing a fixture or `.emod` example that declares specs, or adding a rule that reasons about whether a construct is used.

## `emod fmt` moves a spec to the end of its slice and orders its entries given/when/then
- Type: constraint
- Learning: `writeSlice` appends specs after flows, and `writeSpec` (`internal/formatter/formatter.go:375`) emits `given`, then `when`, then `then`, whatever the source order was. An empty history is written by leaving the line out entirely, so `given []` and no `given` at all both format to no line (`internal/formatter/formatter_test.go` pins both spellings against one expected output). A spec written mid-block between an event and a flow therefore lands at the bottom of the block in `specFormattedEmod` (`internal/cli/fmt_test.go`), and a doc or example that shows a spec in source position is not what `emod fmt` would write. Note the contrast with a context/aggregate entry, which goes directly after `description` and above the nested blocks: a new slice-level kind appends at the end instead.
- Apply when: writing a fmt golden or a canonical constant containing specs, or adding another entry kind to a slice body.

## A spec's `when` resolves against commands *and* events; `given`/`then` against events only
- Type: constraint
- Learning: `declaresCommandOrEvent` (`internal/validator/validator.go:130`) backs `when` because a command slice's `when` names a command while an automation slice's names the triggering event; `undeclaredSpecEvents` (`:360`) checks `index.eventNames` alone, so `given [BorrowCopy]` is rejected with `event "BorrowCopy" does not exist` even though the model declares that command (verified). Only the loose half is pinned — `internal/validator/validator_test.go:1013` covers a `when` naming a declared event, but no subtest gives a `given` or `then` a name declared as a command, so "tidying" `undeclaredSpecEvents` to use `declaresCommandOrEvent` leaves every test green while widening the language. Add that subtest before touching either lookup, and keep the two deliberately different.
- Apply when: editing spec reference resolution in `internal/validator`, or implementing the proposal's "the `then` shape must match the slice pattern" rule.

## `require.NotEqual` on a stripped twin is satisfiable without stripping anything
- Type: recurring-finding
- Learning: the guard that a differential's twin really differs is weaker than it looks — container-shape churn alone answers it. `slicesWithoutSpecs` (`internal/test/fixtures.go`) leaves a nil list nil for exactly this reason: preallocating `make([]*ast.Slice, 0, n)` would make the copy unequal to the original with no spec removed. Stripping only one of a construct's homes passes it too. So pair `NotEqual` with a positive check on the content that must be gone and the content that must be found: `require.Empty(t, test.DeclaredSpecNames(unstated))` plus `require.Equal(t, test.SpecLibraryLendingSpecNames, test.DeclaredSpecNames(stated))` (`internal/diagram/contract_test.go`), and `require.ElementsMatch` that the text search finds the feature where it does belong before asserting it appears nowhere in the other document (`internal/export/export_test.go`).
- Apply when: writing or reviewing a `WithoutX`/`WithOrdinaryX` twin helper and the differential assertions built on it.

## All three spellings of `reads` resolve, and widening a reference check moves the models first
- Type: constraint
- Learning: `modelIndex.viewNames` is filled in `collect(slice)` (`internal/validator/validator.go`) over every slice of both homes, so a trigger's, an automation's and a translation's `reads` all resolve model-wide — across aggregates, across contexts, and against a `mode dcb` context's own slices. The reference checks in `referenceDiagnostics` funnel through `appendUndeclaredRef(diags, kind, name, pos, declared)`, which carries the empty-name guard, so each of the two US-002 added was one line rather than a copy of the guard/lookup/`errorAt` shape; each diagnostic sits on the *value's* position (`ReadsPos`, never the construct's name) and carries no `RuleName`, since a hard error is not something `emod lint --explain` describes. What the widening actually cost was the sweep: five shared fixtures and two checked-in `all_patterns.emod` files named views their own model never declared, and because `RunValidate` and `RunLint` are both `oracle.Check` and error on any diagnostic whatever its severity, they stopped being models the pipeline accepts the moment the check landed — taking `internal/oracle`'s leaves, roughly forty `internal/cli` subtests, two `internal/lsp` leaves and two picture-and-document guards with them. Three commits of model moves preceded the two-line production change. Note that `AutomationReadsLibraryLending` and `AutomationScheduleLibraryLending` are invisible to a scan for zero-diagnostic assertions: they already carry an expected diagnostic list, so they fail by *gaining* a line rather than by going from empty to non-empty.
- Apply when: adding a cross-reference existence check to `internal/validator`, or estimating the blast radius of any check that turns a previously-accepted model into a reporting one.

## A new optional field ships a six-part fixture kit, not a bespoke model per package
- Type: pattern
- Learning: `AutomationReadsLibraryLending` repeats the kit `SpecLibraryLending` established, and every downstream package consumes it instead of writing its own source: the fixture const in `internal/test/fixtures.go` declaring the feature in both slice homes with one instance omitted *mid-block*; the parsing accessor `…Model(t)` in `internal/test/models.go`; a hand-transcribed `…ViewNames`/`…SpecNames` var in declaration order across both homes; a `Without…` twin; a `Declared…(model)` getter that walks `declaredSlices`; and a zero-diagnostic `oracle.Check` leaf. Twins are built on `copyWithEditedSlices(model, edit func(*ast.Slice))` plus the generic `editedCopies[T](items []*T, edit func(*T)) []*T` — `WithoutSpecs` and `WithoutAutomationReads` are each two lines on it. Two mechanics are load-bearing: `editedCopies` leaves a nil list nil on purpose (preallocating an empty one answers a `NotEqual` guard with nothing stripped), and the copies are shallow, so an edit reaching inside a slice's commands/events/views/automations must nest a second `editedCopies` or it writes through to the model the caller passed and the differential compares a model with itself.
- Apply when: starting the fixture task of a story that adds an optional field to the DSL, or adding a `Without…` twin to `internal/test/fixtures.go`.

## The viewer's save path is `importer.ImportDiagram`, so a diagram-node field owes a read-back
- Type: constraint
- Learning: the viewer exports to `.emod` by handing its nodes and edges to WASM (`internal/wasm/pipeline.go:79`), which runs `importer.ImportDiagram` then `formatter.Format` — `internal/viewer/static/emod-export.js` is fifteen lines of pass-through and contains no per-construct knowledge. So a key `collectSliceNodes` (`internal/export/diagram.go`) writes onto a diagram node but `builder.buildSlice` (`internal/importer/importer.go`) does not read back is silently dropped the moment a user saves from the viewer, and nothing on the export side notices. The guard is the round-trip in `internal/importer/importer_test.go`: `require.Equal(t, source, formatter.Format(importFrom(t, source)))` with `source` already in canonical `emod fmt` form, plus `importExported(t, model)` for a shared fixture read back with its `Declared…` getter and its twin. Note the two channels are separate — a value carried as node metadata survives without any edge, and `reads` *edges* in an imported document fold onto translations only.
- Apply when: adding a field to a diagram-document node in `internal/export/diagram.go`, or reviewing a change that claims the diagram document already carried a key.

## `internal/viewer/static` is a display surface with its own vitest harness
- Type: convention
- Learning: `showDetailPanel` (`internal/viewer/static/ui.js`) renders one section per node type, and a node key no section names is invisible in the viewer however faithfully the Go pipeline carries it — the automation section listed trigger event, command and target context while the trigger and translation sections had already listed `Reads`. Its tests are JS: `internal/viewer/tests/*.test.js`, vitest on jsdom (`internal/viewer/vitest.config.js`), run by `task test:viewer`, which `npm ci`s inside `internal/viewer` and is not part of `task test:unit`. jsdom implements no SVG geometry, so a test calls `installSVGGeometry()` from `./svg-env.js` at module scope, and the spelling in files whose module under test touches geometry is the dynamic `const { UI } = await import('../static/ui.js')` (`visibility.test.js`, `detail-panel.test.js`). `showDetailPanel` had no test at all before `detail-panel.test.js`, so restructuring it belongs in its own commit — a reviewer read the twelve helpers extracted alongside one new row as unprotected churn over field-editor, node-delete and section paths the new test does not cover.
- Apply when: a story adds a field that a user should see in the viewer, or any change under `internal/viewer/static`.

## JSON key order is assertable from the raw bytes — `emittedKeyOrder` already exists
- Type: convention
- Learning: `emittedKeyOrder(t, raw)` (`internal/export/export_test.go`, with the `keyOrderReader` token walker beside it) lists the keys of every named object in the order the writer emitted them, filed under the object's `name`. That closes the gap the JSON/CUE ordering learning above records as uncatchable: a `json*` struct whose fields were transplanted from `internal/cue/schema.cue` order now fails an assertion instead of shipping. The shape to copy: build a model declaring the new construct beside the sibling it should file its keys like, export, and `require.Equal` both key lists (`"name", "position", …_position, reads_position, command_position, …`) — the sibling's list in the same subtest is what makes the expectation non-arbitrary. Decoding into `map[string]any` still loses order, so the read-back helpers (`listsKeyedBy`, `automationReadsByOwner`) answer *content* questions only.
- Apply when: adding a field to a `json*` document type in `internal/export/json.go`, or asserting where a construct files its keys.

## Assert a short keyword in a diagnostic with a `\b`-bounded `require.Regexp`
- Type: convention
- Learning: `on` sits inside `descriptiOn`, `cOntext` and `automatiOn` — all three are words of the very message under test (`expected description, on, trigger, reads, command, or target in automation, got %q`, `internal/parser/parser.go:1101`) — so `require.Contains(t, msg, "on")` passes against a parser that never learned the keyword. Both loops covering the new entry spell it `require.Regexp(t, `\b`+entry+`\b`, diags[0].Message)` (`internal/parser/parser_test.go:2937`, `:3007`). This is a different failure from the shadowing recorded above: there the needle hides inside a needle asserted a line earlier, here inside unrelated words the message has always contained. The remaining short keywords — `and`, `or`, `not`, `tag`, and US-003's `every` — all have the same problem.
- Apply when: asserting that a diagnostic or a generated line names a keyword of about four characters or fewer.

## Rename an AST field without moving a single wire name
- Type: convention
- Learning: `ast.Automation.TriggerEvent` → `OnEvent` touched 23 files, every one of them `.go`. The serialized spellings all stayed: the `json:"trigger_event"` tags in `internal/export/json.go` and `internal/importer/importer.go`, the `cueWriter` line, `internal/cue/schema.cue`, `internal/cue/embed_test.go`, the five `internal/viewer/{static,tests}` JS files reading `node.trigger_event`, and the `automation_trigger` edge type, which keeps its name for good. The receipt that the rename carried nothing is that no `.emod` source, golden or expected constant is edited — a "rename" whose diff moves an expected byte string is a wire rename in disguise. The viewer half is the trap: it is out of the Go compiler's reach and only `task test:viewer` (not part of `task test:unit`) would notice, so renaming the Go field and the wire key in one change breaks JS silently.
- Apply when: renaming a field on an `internal/ast` node, or scoping a story that wants both the Go identifier and the serialized key renamed.

## A commit-message receipt is the commit author's obligation, never an acceptance criterion
- Type: constraint
- Learning: this file asks differential receipts to be stated in the commit message, and decomposition transcribed that straight into a criterion — "No line other than the automation's activation line moves in any golden or canonical constant — state the diff in the commit message". implement-flow commits a task only after it closes, so the diff half verified from `git diff HEAD` while the commit-message half closed with no evidence available at any point (the same root cause as "A task criterion requiring 'committed' output cannot close", reached by a phrasing that reads perfectly natural). Criteria must be checkable against the working tree — "`git diff` moves exactly one line per golden" — and the commit-message wording belongs to the commit step, not to the criterion list.
- Apply when: writing an acceptance criterion whose proof is a diff, a receipt, or anything else that lands in a commit message.

## Every `grammar.js` rule carries a one-line example of its full shape
- Type: convention
- Learning: `editors/tree-sitter-emod/grammar.js` puts a comment above each construct rule spelling the construct out whole — `// slice "name" { ... }` (`:77`), `// trigger Kind "Name" { actor Id reads Id }` (`:168`), `// view Name { fields { ... } subscribes [...] }` (`:199`), `// automation Name { trigger Event reads View command Cmd target context Name }` (`:217`) — sixteen of them. They are the only place the admitted entries are listed in order, because the rule body itself is an unordered `buildDescribedBlock`/`repeat(choice(...))` that deliberately imposes none. Adding an item without editing the comment leaves the file's one description of that construct under-stating it, which a reviewer flagged when `on` joined `automation_definition`; nothing tests the comments, so only the diff catches it.
- Apply when: adding or removing an item in a rule in `editors/tree-sitter-emod/grammar.js`.

## A `Declared…` getter answers `nil` for a fixture that declares none of the construct
- Type: recurring-finding
- Learning: `DeclaredActivationEvents`, `DeclaredAutomationReads` and `DeclaredSpecNames` (`internal/test/fixtures.go`) append only stated entries, so a fixture with no automation reads back `nil` and `require.Equal(t, nil, test.DeclaredActivationEvents(reparsed))` holds even if the writer emits no activation line at all. Two independent reviewers flagged the same instance: a new round-trip table case running `test.SpecLibraryLendingModel` (`internal/formatter/formatter_test.go:913`) whose only added assertion was that vacuous one, and which otherwise cloned the round-trip already asserted at `:746`. The round-trip group keeps one leaf per fixture — fold a new per-fixture assertion into the existing leaf rather than opening a parallel table, and pair the getter only with a fixture whose transcribed `…ActivationEvents`/`…ViewNames`/`…SpecNames` list is non-empty.
- Apply when: asserting on a `Declared…` getter, or adding a fixture case to `internal/formatter/formatter_test.go`'s round-trip group.

## Rename a DSL keyword by accepting both spellings first and letting `emod fmt` migrate the tree
- Type: pattern
- Learning: retiring `trigger` inside an automation runs as an ordered sequence, and the order is what keeps the suite green at every commit. The parser learns the new keyword writing into the *same* AST field, last-wins (`parseAutomation`, `internal/parser/parser.go:1057`), so no fixture, golden, example or corpus case moves; the grammar admits the new item beside the old; the Go field is renamed with no wire name touched; `emod fmt` switches to emitting only the new spelling, which is simultaneously the migration path a user's file has and the mechanical way every `.emod` in the tree is rewritten; only then is the old spelling rejected and the sources moved. Wire, schema and viewer names come after that, separately. Collapsed into one breaking commit instead, every golden, canonical constant, example and corpus case has to move in the same change as the parser, and nothing isolates a regression.
- Apply when: renaming or retiring an emod keyword, or decomposing a story that changes an existing DSL spelling.

## `docs/dsl-reference.md` is the one keyword surface no test reaches, and a retirement story forgets it
- Type: constraint
- Learning: US-002 retired `trigger` inside an automation across parser, grammar, formatter, JSON, CUE, `schema.cue`, importer and viewer over eight tasks and never touched the reference, so `docs/dsl-reference.md` still documents the rejected shape in two places: the Automation Pattern skeleton and its bullets (`:328-340`, `trigger <EventName>` plus "`trigger`: the event that activates this automation" and "`trigger` and `command` are required"), and the Cross-References row for `event <Name>` (`:630`, `automation { trigger <Name> }`). Pasting that skeleton into a file now yields two diagnostics from `emod validate` (verified: `trigger is not an automation entry: name the activation event with on`, then `automation block requires either an on event or an every schedule`). Nothing links the doc to the language — no CI check extracts or validates its snippets — so the sweep list for a keyword change has to name it explicitly, in both the pattern section and the cross-reference table, alongside the editor surfaces the fan-out learning above covers.
- Apply when: adding, renaming or retiring an emod keyword, or writing the task list for a story that changes a DSL spelling.

## Retiring a keyword needs its own parser arm, not the fall-through, and three closing braces as the receipt
- Type: pattern
- Learning: a keyword the lexer still tokenizes must get a dedicated `case` in the entry switch (`parseAutomationEntry`, `internal/parser/parser.go:1063`) — falling to `default` reports the generic "expected description, on, reads, command, or target in automation, got %q", which names no replacement. The arm reports at the keyword token with `p.errorAt` and drains with `p.skipRestOfLineOrBlockEnd(tok)` (`:1498`), and the retired word leaves the `default` message, pinned out with `require.NotRegexp(t, `\btrigger\b`, diags[0].Message)` beside the `\b`-bounded loop over the accepted entries (`internal/parser/parser_test.go:3066`). What proves the drain stopped in the right place is not `require.Len(t, diags, 1)` alone but reading back the entries written *below* the rejected line plus `require.NotZero` on `automation.ClosePos.Line`, `slice.ClosePos.Line` and `model.Contexts[0].ClosePos.Line` (`:2986-3040`) — a drain that eats the `}` leaves those zero while the diagnostic count still looks right.
- Apply when: rejecting a keyword or spelling the parser used to accept, or reviewing a recovery path that skips tokens.

## A key rename owes a retired-key negative assertion on every surface that reads the key
- Type: convention
- Learning: asserting the new key reads back passes just as well against a reader that accepts both spellings, so each of the three surfaces renamed in this run pairs its positive leaf with a retired-key one, and the shape is worth copying wholesale: `internal/cue/embed_test.go:112` vets `fullModelJSON` with the automation re-keyed the old way and requires the failure to name it (this works because `#Automation` and its siblings in `internal/cue/schema.cue` are plain closed definitions, so an unknown key is an error rather than an ignored extra); `internal/importer/importer_test.go:208` builds one document through a `documentKeying(key string)` closure, imports it twice, and requires the retired spelling to yield an empty field; `internal/viewer/tests/detail-panel.test.js:85` shows the em-dash placeholder for a node stating the value under the retired key. Without those, a reader left accepting both keys, or a schema left declaring both, ships green.
- Apply when: renaming a serialized key in `internal/export`, `internal/importer`, `internal/cue/schema.cue` or the viewer's JS.

## A diagram-node key has three readers, and they must move in one commit
- Type: constraint
- Learning: the key travels on struct tags alone — `jsonDiagramNode` (`internal/export/diagram.go:20`) writes it, `diagramNode` (`internal/importer/importer.go:35`) reads it, and `internal/viewer/static/ui.js` reads it a third time in `showDetailPanel`; no Go code in between mentions the string. Tasks 7 and 8 moved the two Go tags together for exactly the right reason (an exporter and importer keyed differently drop the value on the viewer's save path, silently) and then left the JS a commit behind, so the tree at `949b102` exports `on_event` while the panel reads `node.trigger_event` and shows `—` for every automation's activation event — with `task test:unit` and `task test:viewer` both green, because the JS test fed the old key too. Rename all three in one change, or the intermediate commit is a user-visible regression no suite can see.
- Apply when: renaming a key on a diagram-document node, or decomposing a wire-rename story into Go and viewer tasks.

## Parser diagnostics at a stored `ast.Position` go through `p.errorAtPosition`
- Type: convention
- Learning: `errorAtPosition(pos ast.Position, msg string)` (`internal/parser/parser.go:1525`) is the single constructor for a diagnostic reported away from the current token — the block-completeness checks in `decides_on`, `view`, `automation` and `translation` all call it with the position they stored (`:699`, `:702`, `:1014`, `:1054`, `:1057`, `:1178`, `:1181`, `:1184`), and `errorAt(tok, msg)` now delegates through it. Those eight sites were hand-built `&diagnostic.Entry{Filename: x.Pos.Filename, Line: …, Column: …, Message: …}` literals; a ninth copy is six lines that can silently take `p.filename` instead of the stored position's filename, or omit a field a future `Entry` gains.
- Apply when: adding a "block requires X" check or any parser diagnostic anchored to a position captured earlier rather than to `p.peek()`.

## Read a decoded export document back with `objectsUnder`/`statedUnder`, in the writer's slice order
- Type: pattern
- Learning: `internal/export/export_test.go:4177-4220` now has the generic walkers a read-back needs — `objectsUnder(parent, key)` casts a `[]any` of objects, `exportedSlices(doc)` visits each context's aggregates' slices *before* the slices that context declares directly (the order the writer emits, and the order a hand-transcribed `…ViewNames`/`…ActivationEvents` list is written in), `exportedAutomations(doc)` composes the two, and `statedUnder(objects, key)` collects only the objects that state something under the key. That last one is what makes `require.Equal` against a transcribed list mean something: padding silent objects with placeholders would turn the assertion into a count. `diagramAutomationReads` was rewritten onto `statedUnder`, so the bespoke `for _, node := range nodesOfType(...)` walker is no longer the shape to copy.
- Apply when: asserting that an export or diagram document carries a per-construct value, or adding a read-back helper to `internal/export/export_test.go`.

## A serialized key spells the DSL keyword; the Go field may name the concept
- Type: convention
- Learning: `ast.Automation.Schedule`/`SchedulePos` ship as `every` and `every_position` on every wire — `jsonAutomation` (`internal/export/json.go`) and `cueWriter.writeAutomation` (`internal/export/cue.go`), `#Automation` in `internal/cue/schema.cue`, `jsonDiagramNode`, the importer's `diagramNode`, and `node.every` in the viewer's JS — exactly as US-002's `OnEvent` ships as `on_event`. The Go identifier is free to describe the concept, but a key spelled after the field (`schedule`) splits the wire vocabulary from the language, and three negative leaves exist to catch it: `internal/cue/embed_test.go:123` re-keys the value `schedule` and requires `cue vet` to fail naming it, `automationNodeKeying` (`internal/importer/importer_test.go`) imports one node under each spelling and requires the wrong one to arrive empty, and `internal/viewer/tests/detail-panel.test.js` shows the em-dash for a node stating the value under a key the exporter never writes. A position key takes the keyword spelling plus `_position` (`every_position`, never `schedule_position`).
- Apply when: choosing the JSON, CUE or diagram-node key for a new DSL entry, or reviewing a field addition to `internal/export`, `internal/importer`, `internal/cue/schema.cue` or the viewer.

## An exactly-one-of block rule reports on top of the malformed entry's own diagnostic
- Type: constraint
- Learning: `parseAutomation` (`internal/parser/parser.go:1050`) checks the pair after the block closes — "automation block cannot declare both on and every" / "requires either an on event or an every schedule", both at the automation's name — and a malformed entry records no value, so `every nightly` as an automation's only activation line reports twice: once at `nightly`, once for the arity. `on "RoomReserved"` behaves identically, and the retired `trigger <Name>` is pinned at `require.Len(t, diags, 2)` in `internal/parser/parser_test.go`. Suppressing the arity report for one of them would make that keyword the one entry that counts as an activation form without recording one, and applying the suppression uniformly breaks the pinned trigger leaf. A test or acceptance criterion that wants exactly one diagnostic out of a malformed activation line must therefore give the block a valid sibling activation entry — the `every naming a bare identifier` fixture writes `on RoomReserved` below the offending line for that reason.
- Apply when: writing a criterion or a `require.Len(t, diags, 1)` assertion for a malformed entry of a construct whose entries carry a required-one or exclusive-choice rule.

## A mutually exclusive alternative has no stripped twin, and the importer must not fold one in
- Type: pattern
- Learning: an automation states `on` or `every`, never neither and never both, which costs two things the optional-field fixture kit takes for granted. There is no `WithoutSchedules` twin — deleting the schedule leaves a model the parser rejects — so the "nothing else moves" receipt is built by *swapping* the alternative rather than removing it: one model rendered twice differing only in which form a single automation states (`sweepingModel`, `internal/diagram/contract_test.go`; `pairedAutomations(schedule)`, `internal/viewer/tests/renderer.test.js`), with the featured twin first required to show the value so the comparison is not two identical pictures agreeing. And `foldEdges` (`internal/importer/importer.go:270`) fills `OnEvent` from an `automation_trigger` edge only when *both* forms are empty: an edge drawn onto a scheduled automation would otherwise fold into text `emod validate` rejects, which its leaf proves by re-scanning the formatted model instead of comparing fields.
- Apply when: adding an entry that is one of a mutually exclusive pair, or building the fixture kit, the differential receipt or the importer folding for one.

## A `*_position` key needs its value pinned to its own AST position
- Type: recurring-finding
- Learning: `emittedKeyOrder` pins where a position key files among its siblings and the text-search leaves pin that it appears, but both stay green when the key is wired to the wrong position — `SchedulePosition: convertPosition(a.OnEventPos)` would satisfy every assertion the `every_position` task added. The subtest that fails on a mis-wiring is "includes positions for automation name/activation event/reads/command/target and braces" (`internal/export/export_test.go`), which asserts the line and column of each of an automation's position keys, so a new `*_position` key belongs in that list and not only in the key-order expectation.
- Apply when: adding a `*_position` key to a `json*` document type in `internal/export/json.go`.

## A quoted or identifier block entry is one `case` on the `parse*EntryInto` family
- Type: convention
- Learning: `parseQuotedEntryInto(construct string, value *string, position *ast.Position)` (`internal/parser/parser.go:1396`, formerly `parseDescriptionInto`) and its sibling `parseIdentifierEntryInto` are the whole of an entry's parse-and-recover: advance the keyword, require the value token, `errorAt` the offending token, then `skipRestOfLineOrBlockEnd(keywordTok)`. The message interpolates the *consumed keyword's* value — `expected quoted string after %s in %s, got %q` — so `every` joined `description` and `external_system` as a caller without moving a byte of any existing diagnostic; hardcoding the construct's first keyword there would have made the schedule's error read "after description". Add a keyword by adding a `case` that calls one of the two: a hand-rolled advance/check/`errorAt` block drifts on the drain, and the drain is what keeps one malformed line from cascading into a diagnostic per remaining word.
- Apply when: adding a keyword entry to a block construct in `internal/parser/parser.go`.

## The viewer shows a node twice — the canvas box and the detail panel
- Type: convention
- Learning: a diagram-node key a user should see needs `appendBlockLabels` in `internal/viewer/static/renderer.js` — the box's drawn labels, plus the `<title>` inserted as the group's *first* child, which is where a group's hover tooltip is read from — as well as the `showDetailPanel` row in `ui.js`; they are separate readers, and a panel-only change leaves the automation drawing a bare name on the canvas. Assertions on a drawn label must walk the group's `<text>` elements (`drawnLabels`/`drawnText`, `internal/viewer/tests/renderer.test.js`), because `group.textContent` folds the `<title>` in and passes on the tooltip alone. The receipt that a second line fits the box is `drawnBoxes(svg)` — x, y, width, height and fill per node id — compared across a render with and without the value, after the badge's `y` is required to sit between that box's own top and bottom edge.
- Apply when: surfacing a new node field in the viewer, or asserting on what the viewer's SVG draws.

## A task's change-set assertion must name every file its own patterns require it to change
- Type: recurring-finding
- Learning: Task 8 of US-004 asserted "the only files this task changes under `editors/tree-sitter-emod` are `grammar.js` and files under `test/corpus/`" while its Patterns section said the task MUST delete `queries/highlights.scm:60` — a contradiction that forced a plan-impact finding. The two halves could have agreed from the start, because the pattern note also explained why the query file had to move: once `trigger_definition` stops admitting an `identifier`, the highlight pattern becomes impossible and tree-sitter refuses to compile the whole query file. A destructive pattern that touches a file outside a task's listed change-set is not a nit; the change-set assertion and the pattern are the two places an author reads what the task does, and a reviewer of the finished commit reads only the first.
- Apply when: decomposing a task with a "only these files change" assertion, or writing a pattern note that requires editing a file the task's change-set list does not name.

## A deletion's Verification grep can false-positive on kept wire-key assertion helpers
- Type: recurring-finding
- Learning: US-004 Task 9's verification `rg -n 'KindPos|Trigger\{[^}]*Kind' -g '*.go'` matches `_, hasKindPosition := tr["kind_position"]` locals in `internal/export/export_test.go` and `internal/cli/export_test.go`, because `KindPos` is a substring of `hasKindPosition`. Those helpers deliberately assert the retired *wire key* `kind_position` is absent (a task-carrying obligation from the earlier wire-key removal) and never reference the deleted AST field — the compiler proves the field's references are gone, and renaming them would churn tests guarding a different layer for no signal. Judge the criterion's intent (no field references, compiler-proven) over the literal grep, and record the decision instead of renaming.
- Apply when: writing a deletion task's verification grep, or closing a task whose grep matches identifiers that merely share a substring with the removed field.

## `emod fmt <file>` writes in place, so a receipt run dirties the working tree
- Type: constraint
- Learning: `emod fmt <file>` formats the file in place (gofmt `-w` style) and prints nothing to stdout — `--check` is the only flag that avoids the write. A byte-identical receipt that runs `emod fmt` against a real example or fixture therefore modifies that file in the working tree, and when the file was not already canonical (a pre-existing state several `.emod` files here are in) the diff is a wholesale reformat unrelated to the change under test. Copy the fixture to a temp path first, or `git checkout --` it afterwards.
- Apply when: verifying formatter output as part of an evidence receipt, or diffing the tree after any `emod fmt` invocation.

## An arrow between two constructs is drawn by six surfaces, and none of them reads another
- Type: pattern
- Learning: a relationship the diagram should show has to be taught to each drawer separately. `convertModelToDiagram` (`internal/export/diagram.go:257`) writes the document edge; `internal/diagram/svg.go` and `internal/diagram/drawio.go` each walk the AST themselves and never read that document, so an edge added to it draws nothing in either picture; `internal/diagram/mermaid.go` and `ascii.go` are two more independent walks (both still show `reads` for a translation only, and their unchanged bytes are the receipt that a picture-only change stayed in the pictures); `foldEdges` (`internal/importer/importer.go:270`) reads the edge back on the viewer's save path; and the viewer needs *two* entries, `EDGE_TYPE_BY_ENDS` in `static/model.js:141` to type an arrow the user drags and `edgeConfig` plus `arrowClassMap` in `static/config.js:30,40` to paint it. Both viewer misses are silent: `renderer.js:320` does `var cfg = edgeConfig[edge.type]; if (!cfg) return;` and then `if (!srcPos || !tgtPos) return;`, so an unknown edge type and an endpoint id no node carries are each dropped with no error — which is also why every writer guards the name against the declared-view map (`appendReadsEdge`, `export.go:1142`; the `readsEdge`/`readsArrow` closures at `drawio.go:467` and `svg.go:176`) rather than emitting an edge onto an id nothing drew.
- Apply when: adding a relationship between constructs to the diagram, or extending an existing edge type to a construct that did not have it.

## `EDGE_TYPE_BY_ENDS` carries only the direction the exporter writes
- Type: constraint
- Learning: the map is keyed `"<sourceType>><targetType>"` and consumed as `EDGE_TYPE_BY_ENDS[src.type + ">" + tgt.type] || "flow"` (`internal/viewer/static/model.js:158`), so each pairing is one-way on purpose. `reads` is written source=view, target=reader by the exporter and folded off the *source* label by the importer, so `"view>trigger"` and `"view>automation"` belong in the map and `"trigger>view"` must not: an arrow typed on the way in is folded by nothing, and the construct comes back off a save reading nothing while the user sees an arrow. The pin is a negative `it.each` table beside the positive one — "does not type a %s to %s arrow as %s, the direction the exporter never writes" (`internal/viewer/tests/model.test.js`).
- Apply when: adding a pairing to `EDGE_TYPE_BY_ENDS`, or reviewing a change that makes an edge type symmetric.

## An importer fold must re-check the endpoint node types, not only the edge type
- Type: constraint
- Learning: the viewer gives every arrow endpoint handles for repointing (`internal/viewer/static/renderer.js`), and a repointed arrow keeps the type it was given, so `foldEdges` can be handed a `reads` edge whose source is a command or an event. `readsDrawnFromView` (`internal/importer/importer.go:300`) therefore takes the label only when `src.Type == "view"`, and only when the node recorded no reads of its own, so node metadata beats a stale wire; without the type check a save writes `reads RecallCopy` onto an automation, which is a view reference `emod validate` rejects — the receipt is re-scanning the *formatted* model rather than comparing fields. The translation arm at `:283` is deliberately left unchecked, so it is not the shape to copy.
- Apply when: adding or editing a `case` in `foldEdges` (`internal/importer/importer.go`), or wiring a new edge type into the viewer's save path.

## Allocate a draw.io cell id only once the cell is certain to be written
- Type: constraint
- Learning: `allocID()` in `ExportDrawio` (`internal/diagram/drawio.go`) is a running counter shared by every vertex and edge, so an id taken before a guard that may skip the cell renumbers every later cell in the file. The `readsEdge` closure (`:467`) resolves the view and returns `""` *before* calling `allocID()` for exactly this reason: a model naming a view no slice declares would otherwise shift the ids of every subsequent edge, and a "nothing else moves" differential against such a model fails on ids alone even though no box or arrow changed.
- Apply when: adding a conditionally-emitted cell to `internal/diagram/drawio.go`.

## Read an SVG or draw.io arrow back as the two boxes it meets
- Type: convention
- Learning: `internal/diagram/contract_test.go` has `diagramConnection{source, target, paint}` (`:53`) — an arrow named by the labels of the boxes at its ends, plus the string it is painted with — and both exporters have a resolver into it: `svgConnections` (`internal/diagram/svg_test.go:438`) maps each path endpoint to the box whose centre sits at that point and fails loudly when no box does, and `drawioEdges` (`internal/diagram/drawio_test.go:881`) maps an `mxCell` edge's `source`/`target` ids back to the labels of the cells they name. Assert connections by box label, and assert appearance by comparing a new arrow's paint against an arrow of the same kind already drawn in the *same* render (the translation's `reads` arrow was the reference for the trigger's and the automation's), never by restating coordinates, cell ids or a style string — those move with layout and with the style table.
- Apply when: asserting what an arrow connects or how it is painted in `internal/diagram`'s SVG or draw.io output.

## A "the save is text emod accepts" receipt must keep the parse diagnostics
- Type: recurring-finding
- Learning: `savedModelDiagnostics` (`internal/importer/importer_test.go`) returns `validator.Validate(saved)` and throws away the scan/parse half that `parseSaved` hands back beside the model, so it answers only for the validator layer. That matters because the hand-built documents in this file are minimal on purpose — automations stating neither `on`/`every` nor a command, views with no `fields` — and their formatted text carries parse diagnostics of its own (five, for the document at `:583`), so the helper reads clean over text `emod validate` would reject, and would go on reading clean if parse recovery ever dropped the construct under assertion. Either assert with a whole-pipeline check (`oracle.Check(source, filename)` runs lexer, parser, validator and linter) over a document complete enough to be one, or word the message for the layer the helper actually runs.
- Apply when: writing a receipt that a viewer save produces valid emod, or splitting a parse-and-check test helper into two.

## A second construct gaining the same field extends the existing fixture, with independent twins
- Type: pattern
- Learning: when `reads` had to answer for triggers as well as automations, the fixture kit grew sideways rather than sprouting a second model: `TriggerReadsLibraryLending` (`internal/test/fixtures.go`) declares both constructs in both slice homes and ships *two* transcriptions (`…TriggerViewNames`, `…AutomationViewNames`), two getters (`DeclaredTriggerReads`, `DeclaredAutomationReads`) and two twins (`WithoutTriggerReads`, `WithoutAutomationReads`) over the one model. The twins must be independent — each clears only its own construct — and that independence is itself asserted: the formatter round-trip runs both twins over the fixture showing one leaves the other's reads whole, and every differential states each twin's emptiness *and* the reading model's transcribed list before comparing, so a strip reaching both constructs, or neither, cannot pass. A twin that cleared every `reads` would make the picture differentials unable to say which construct's arrow appeared.
- Apply when: extending an existing optional field to a second construct, or adding a twin to a fixture that already has one.

## `ExportSVG` and `ExportASCII` ignore the `Style` they are handed
- Type: constraint
- Learning: only `ExportMermaid` (`internal/diagram/mermaid.go:18`) and `ExportDrawio` (`drawio.go:105`) branch on the style; `ExportSVG` (`svg.go:12`) and `ExportASCII` (`ascii.go:21`) declare it `_ Style` and always draw the standard layout — SVG writes its four lanes unconditionally (`Wireframes`, `Commands / Views`, `Events`, `External Systems`), while draw.io has three lane sets (standard; DCB's `Triggers / Commands` + `Events` + `External Systems`; projected's per-tag lanes). So a DCB- or projected-only obligation can be pinned only in draw.io and Mermaid — US-006 Task 1's non-overlap leaf for those layouts is a draw.io leaf for exactly that reason — and an SVG or ASCII subtest parameterised over `diagram.StyleDCB`/`StyleProjected` asserts nothing its `StyleAuto` case did not, so it cannot fail on a style regression.
- Apply when: writing a style-dependent acceptance criterion or test in `internal/diagram`, or asking why a DCB/projected layout change is invisible in the SVG.

## SVG and draw.io share box placement, and share it through `drawio.go`
- Type: pattern
- Learning: box geometry is one implementation both picture formats call, and it lives in `internal/diagram/drawio.go` despite the file name: `laneRowY(laneY)` (`:705`, where a lane's row of boxes starts, below the 30px strip carrying the lane label), `itemLayout(usableW, numItems, index, sliceX)` (`:749`), and `reactorBoxes(s, cmdViewLaneY, sliceX, lineBreak)` (`:722`, the automation and translation-reactor row, taking the format's line break as a parameter — `"\\n"` for draw.io, `"\n"` for SVG). `svg.go:50-53,126` calls all three; before US-006 the same arithmetic was written four times. Two consequences: do not re-spell placement per format, and a change to any of those helpers moves *both* pictures, so its receipt must render SVG as well as draw.io. This does not extend to arrows — each format still routes and paints its own, as the six-surfaces learning says.
- Apply when: changing where a diagram box sits, or giving a construct a box in both picture formats.

## Assert diagram box placement relationally, by label
- Type: convention
- Learning: `internal/diagram/contract_test.go` reads a picture back as `[]diagramBox{label, appearance, rect}` for both picture exporters (`svgBoxes`, `drawioBoxes`; the text exporters leave the table's `boxes` field nil and the placement test skips them) and states placement as relations over those rects, never as coordinates: `labelsWithin(boxes, container)` — a lane is itself a box, `boxLabelled(t, boxes, "Commands / Views").rect` — `labelsBelow(boxes, lowestEdge(rectsLabelled(t, boxes, names)))`, `boxesDrawnOver(rects, names)` for pairwise overlap, and `gearedBoxes(boxes)` to select the automations and translation reactors by their gear marking. Each assertion is `require.Equal` against a transcribed label list, so a box that moved *out* of the lane fails as loudly as one that never arrived, and the failure names the two boxes that collided. A y coordinate or a lane index restated in a test moves with every layout constant.
- Apply when: asserting where a box is drawn in SVG or draw.io output, or pinning that a construct changed lanes.

## `computeLayout`'s `minW` must cover every child it placed, not the column band
- Type: constraint
- Learning: `layoutSlice` (`internal/viewer/static/layout.js`) stacks a slice through two helpers — `placeColumn(items)` centres one box of `sBoxWidth` per row, `placeRow(items)` spreads several across `sSliceWidth - 20`, which is *wider* than that band — and the stack is now five calls in draw order (triggers, processors, commands, events, views), so a row reorder is a reorder of those five lines. `minW`/`minH` are the slice's size at rest and `interaction.js:154` resizes the box live from `minW` during a drag, so a two-box row silently drew 10px outside the slice with nothing dragged. `minW` therefore starts at `sSliceWidth` and takes `max(p.x + p.w + L.slicePad - xPos)` over every placed child; a new `placeRow` call, or a row that can hold more items, must stay inside that maximum.
- Apply when: adding or widening a row in the viewer's slice stack, or touching `minW`/`minH` in `layout.js`.

## A reorder receipt asserts the sequence, not the gaps between it
- Type: recurring-finding
- Learning: a leaf named for an order that only compares consecutive deltas passes under the exact reversal the task was about — `stepsDown(...)` over `['trg1','cmd1','evt1','view1']` in `internal/viewer/tests/slice-stack.test.js` yields one equal step per band whichever direction the stack runs, so "stacks the trigger, command, event and view evenly" verifies the empty-row early return and nothing about order (raised as an advisory on US-006 Task 3). The order claim needs the positions themselves: chained `expect(a.y).toBeLessThan(b.y)`, as the sibling leaf does, or the ids sorted by y compared with `toEqual` against the expected sequence. Keep the even-step comparison for what it does prove — that an absent row costs no band.
- Apply when: writing the receipt for a layout reorder, or reviewing a test that asserts spacing, deltas or uniformity where its name claims order.

## Strengthening a test to a whole-sequence `require.Equal` means deleting the subtest it subsumes
- Type: recurring-finding
- Learning: "trigger renders as ui timeframe however its name reads" (`internal/diagram/mermaid_test.go`, `Contains`/`NotContains`, `StyleAuto` only) outlived the "every style" subtest that began asserting `require.Equal(t, timeframeSlotAssignments, nonEventTimeframes(t, output))` across all three styles, which pins every claim the older leaf makes and more. Three independent reviewers raised it, an implementer reported it fixed, and the deletion did not land — a self-reported fix is exactly what an evidence gate cannot check, which is why a carried finding needs a disposition naming the concrete change. When a task adds a structural assertion over the complete ordered output, the narrower `Contains`-based leaf is not extra safety: it is a parallel change-detector that fails twice for one regression and drifts on the next rename. Fold its one unique input flavour into the new fixture (an extra trigger name in `timeframeSlotModel`, here) and delete the subtest.
- Apply when: adding a table or whole-output `require.Equal` subtest over a surface an existing subtest already checks with `Contains`, or closing a review finding that calls a test redundant.

## The element palette is written down four times and pinned by three Go tests
- Type: constraint
- Learning: six element types — trigger, command, event, view, automation, translation — each own one fill and one stroke, stated in four places: the colour consts in `internal/diagram/drawio.go` (which `svg.go` draws from too), `nodePalette` in `internal/viewer/static/config.js`, the per-class `:hover` and `.hl.<type>-block rect` rules in `internal/viewer/static/viewer.html`, and the table in `docs/dsl-reference.md` section 13. Three tests in `internal/diagram/contract_test.go` hold them together: `TestExporterPalette` over the exports themselves, `TestExporterPalettePinsViewer` against `viewerNodePalette` (which reads `../viewer/static/config.js`), and `TestExporterPaletteMatchesReference` against `dslReferencePalette` (which reads `../../docs/dsl-reference.md`) — so retuning a colour on one surface alone fails `task test:unit`, and an edit to the viewer's JS breaks a *Go* test although `task test:viewer` is a separate target. `viewer.html` is not covered there: its selected-state rules resolve to one shared `palette.highlightFill` rather than a per-type shade, and are pinned only by the JS leaves that read those rules out of the stylesheet, so repainting a type in `nodePalette` without moving its hover and selection rules leaves a stale hue that only `task test:viewer` catches. Hue-family distinctness across the palette is owned on the Go side, through `colorFamily` in `internal/diagram/contract_test.go`.
- Apply when: retuning any diagram colour, adding an element type, or wondering why a colour edit fails a test in another package.

## `docs/dsl-reference.md` section 13 is machine-read, so its table's spelling is load-bearing
- Type: constraint
- Learning: the reference is otherwise the one surface no test reaches; the palette table is the exception. `dslReferencePalette` (`internal/diagram/contract_test.go:1349`) reads `../../docs/dsl-reference.md`, locates the section by its `Diagram Palette` heading — failing with "docs/dsl-reference.md must contain a 'Diagram Palette' section" when it cannot — and parses the rows beneath it. The table is four columns, `| Element | Fill | Stroke | Notes |`, its values bare `#rrggbb` with no backticks, and each element spelled as the viewer's `nodePalette` key. Renaming the section, relabelling a row ("read model" for "view") or deleting one breaks the parse. The section is *appended* as 13 rather than inserted, which is how a numbered section is added without renumbering headings or invalidating the number-prefixed in-document links.
- Apply when: editing, reformatting or reordering `docs/dsl-reference.md`, or adding a second machine-read table to a doc.

## A cross-language expectation reader must refuse a partial table, never return it short
- Type: convention
- Learning: when the expected value has to come from a file written in another language (taking it from the exporters' own constants would compare them against themselves), the reader must fail on anything it cannot parse whole, because a per-key comparison against a map that came back short simply agrees over the keys it left out. `viewerNodePalette` (`internal/diagram/contract_test.go:1305`) is the shape: it requires the file to contain `nodePalette`, requires that block to terminate, then `require.Len(t, matches, 6, "nodePalette must list exactly six element types, got %d", len(matches))` — a length check that refuses a table one row short rather than returning it. Each refusal names the file and what it expected, so the failure points at the source rather than at the comparison downstream. The relative paths (`../viewer/static/config.js`, `../../docs/dsl-reference.md`) resolve because `go test` runs in the package directory.
- Apply when: pinning a Go value against a config, table or doc written in another language, or adding such a reader to `internal/diagram/contract_test.go`.

## One fill and stroke constant per element type — never named after the shape, never aliased
- Type: convention
- Learning: naming a colour constant after the shape two element types share paints them alike and leaves neither able to be repainted apart. Each type names its own in `internal/diagram/drawio.go` — `fillAutomation`/`strokeAutomation` beside `fillTranslation`/`strokeTranslation` — and both picture formats place them through `reactorBoxes`, the shared helper whose `reactorBox` carries `isAutomation` so each renderer selects the pair for the type it is drawing. `fillTranslation` repeats `fillExternal`'s value (`#f5f5f5`) rather than aliasing it: a `simplify`-style pass folding two equal hex constants into one recreates the defect. The external system a translation names is not an element type and holds no palette entry of its own: it is told from the translation reactor sharing its grey by its dashed outline alone, which a leaf states as a two-key map rather than a lone `Contains(output, "dash")`.
- Apply when: adding a colour constant to `internal/diagram/drawio.go`, or de-duplicating two constants that happen to hold the same value.

## A trigger's header bar is a second rect, and the two renderers order it oppositely
- Type: constraint
- Learning: a trigger is drawn as a screen — a small header bar inside the top edge — in both the SVG exporter (`svgTriggerRect`, `internal/diagram/svg.go:275`) and the viewer (`internal/viewer/static/renderer.js:308`). The bar is a `<rect>` in both, and each renderer orders it against the main box for its own reason, so the two orders are deliberately opposite and neither may be "tidied" to match the other. `svgTriggerRect` writes the header *first* and the box second, because the text label that follows attaches to the rect immediately before it — emit the header last and the label binds to the framing instead of the box. The viewer appends the main rect *first* and the header second, because `drawnBoxes` (`internal/viewer/tests/renderer.test.js:241`) reads a group's first rect as the block, which the leaf at `:362` pins by name. draw.io draws no framing at all: `styleTrigger` (`internal/diagram/drawio.go:98`) carries fill, stroke and font only, so a trigger there is a plain box.
- Apply when: adding a framing, badge or second shape to a diagram box in `internal/diagram/svg.go` or `internal/viewer/static/renderer.js`.

## `svgPicture` sees labelled boxes and arrows only, so it is not the receipt for a new mark
- Type: recurring-finding
- Learning: `svgPicture` (`internal/diagram/svg_test.go:531`) is documented as returning "everything the diagram draws" and backs the "prose must not add, move or repaint a shape" receipt at `:301`, but it is `svgBoxes` plus `svgArrows`, and it pairs each box with the text on it. An unlabelled mark — the trigger's header rect is the one that exists — falls outside it, so that comparison stays green if such a mark moved, was repainted or disappeared. `svgShapes` (`:354`) walks the boxes in document order and is the closer starting point when a receipt has to cover a mark rather than a labelled box. The viewer has the same trap: `drawnBoxes` (`internal/viewer/tests/renderer.test.js:241`) reads a group's first rect only, by design, so it reports the block and never the framing.
- Apply when: leaning on a "nothing else moves" receipt in `internal/diagram/svg_test.go`, or teaching a picture exporter to draw a shape that is not a rect.

## A viewer leaf must be able to fail only for the paint its name blames
- Type: recurring-finding
- Learning: a leaf asserting over colours its own change does not own fails for the wrong reason — "no two types select alike" pinning the six `.hl.<type>-block rect` rules from inside a task that only framed the trigger, or "gives the trigger the colour of the slice it sits on" comparing the trigger's fill against the *slice background's*, so recolouring the slice fails a leaf named for the trigger. Two shapes fix this. A count-based invariant splits in two, each with one reason to fail: "repaints a selected block" beside "leaves no two node types selecting alike" — a count says neither on its own. And a leaf reads the rule it blames straight out of the stylesheet and compares that one value, the way `internal/viewer/tests/renderer.test.js:551` reads `.hl.<type>-block rect` and requires `palette.highlightFill`, so the failure names the rule that drifted rather than only that two colours differ. Hue-family distinctness across the palette has its owner in Go (`colorFamily`, `internal/diagram/contract_test.go`); a per-type viewer leaf restating it adds no coverage.
- Apply when: adding a leaf to `internal/viewer/tests/renderer.test.js`, or asserting a colour relation between two elements.

## `internal/lsp` walks the model once now — extend `model.go`, never open a sixth walk
- Type: pattern
- Learning: `internal/lsp/model.go` is the package's only AST traversal. `parseModel(text, uri)` replaces the `lexer.Scan` + `parser.New(...).Parse()` opening every feature used to spell; `scopedSlices` yields both slice homes as `scopedSlice{slice, context, aggregate}` with `aggregate == nil` for a `mode dcb` context's own slices (which is what `declaredIn` in `hover.go` renders as a bare context name instead of `Ctx > Agg`); `declarationsIn`/`declaredNamesInOrder`/`newDeclaredNames` carry every command, event, view and context name with its position; and `referencesIn` states every site that *names* one — a view's `subscribes`, an automation's `on`/`reads`/`command`/`target context`, a translation's `reads`/`command`, a trigger's `reads`, a flow's command and event. `internal/lsp/position.go` holds `cursor`/`cursorAt`/`onName` plus `nameRange`/`locationFor`. All five features consume them: `GetDefinition` is a loop over `referencesIn`, `GetReferences` uses the same list for both `targetAt` and the listing (so a new reference site gives jump and listing at once — US-009 Task 5's whole production diff was one `add(viewName, auto.Reads, auto.ReadsPos)` line), `GetHover` and `GetSemanticTokens` range over `scopedSlices`, `GetCompletions` calls `declaredNamesInOrder`. This closes two gaps earlier entries in this file record as open: every LSP walker being blind to `ctx.Slices`, and `isKeyword`'s ordinal range in `hover.go` — hover now gates on `tok.Type.IsKeyword()`. `keywordDescriptions` (`hover.go`) and the lists in `completer.go` remain the hand-maintained keyword surfaces.
- Apply when: adding or changing any feature in `internal/lsp`, or teaching the editor about a construct or block entry that names another construct.

## The completer reads raw lines, not the AST, so a new block owes two arms and a value-slot decision
- Type: constraint
- Learning: `GetCompletions` never asks the parser where the cursor is. `blockScanner` (`internal/lsp/completer.go:59`) walks the lines up to the cursor keeping a stack of `blockContext`, over `codeOutsideStringsAndComments(line)` so a `#`, `//` or brace inside a quoted description opens and closes nothing. A block needs *both* a `findBlockKeyword` arm (`:124`, spelling → `blockContext`) and a `keywordsFor` arm (`:217`, `blockContext` → labels); one without the other silently falls through to the parent's list, which is exactly how an automation body offered `model, actor, context`. `keywordAwaitingBrace` holds a braceless keyword only until the next line carrying code, so `command Ship` inside an automation stays a reference rather than opening a command block for the rest of the body. Value completions come from `valueSlots` (`:170`), an entry-keyword → `{nameKind, CompletionItemKind}` map holding `on` and `reads` only; it is skipped inside `ctxFields` because keywords stay legal as field names, types and modifiers (`id reads required` names no view), and its item kinds deliberately match the types `GetSemanticTokens` paints the same names with (event → `EventCompletion`, view → `ClassCompletion`). `trigger`, `view`, `translation`, `flow` and `spec` bodies still have no arm and still answer the top-level list.
- Apply when: teaching `internal/lsp/completer.go` a new block, or a new entry whose value names a declared construct.

## An `assert…` helper that returns the value it pinned whole makes every follow-up check dead
- Type: recurring-finding
- Learning: `assertHover` (`internal/lsp/hover_test.go:60`) ends `require.Equal(t, expectedContent, hover.Contents.Value)` and then `return hover.Contents.Value`, so the caller's `require.Contains`/`require.Regexp`/`require.NotContains` on the returned `content` (`:175-178`, `:200-202`, `:214`) inspect a string byte-identical to the literal the caller just passed in — they can fail only if the test's own literal is self-contradictory, never for a production reason. Two independent reviewers raised it on one task. The trap is that both halves come from advice in this file: the `\b`-bounded regexp for a short keyword like `on`, and the whole-value `require.Equal`. They do not compose — pick one per leaf. If the leaf pins the exact wording, the regexps and `NotContains` add nothing and should not be written; if the wording should stay free to change, the helper must assert only the shape (kind, non-nil, range) and let the leaf assert the words. The same applies to acceptance criteria: a criterion that names both an exact expected string and substrings it must contain is asking for a dead assertion.
- Apply when: writing a test helper that both asserts and returns a value, or writing an acceptance criterion that names both an exact expected string and fragments of it.

## A `_test.go` file always carries the `Test…` umbrella for the name it wears
- Type: convention
- Learning: every `_test.go` under `internal/` either pairs with a same-named source file or, where it does not (`internal/diagram/contract_test.go`, `internal/diagram/style_test.go`, `internal/parser/integration_test.go`), still holds the `Test<Thing>` umbrella for the thing its name states — `style_test.go` names the production type `Style` (declared in `drawio.go`) and holds `TestStyle`. `internal/lsp/cursor_test.go` is the sole file in the repo with no `Test` function at all, and it takes the name of `cursor`, a production type declared in `internal/lsp/position.go`, while holding only the `posIn` document-position helper — so it reads as that type's unit test and a reader looking for one finds nothing. A helper shared by several test files in a package belongs in a file named for the helper's role, or in the umbrella that already owns it.
- Apply when: extracting a helper shared across several `_test.go` files in one package, or naming a new test file.

## A tested, defensible improvement found on the way is still a separate commit
- Type: convention
- Learning: two of US-009's five tasks drew a scope advisory for a change that was correct, tested and outside the task's stated boundary — the completer task, whose boundary said "only which keyword list is chosen changes", also taught the scanner string-literal and `#`-comment awareness; the `reads` task, whose affected set was `definition*`/`references*`, also consolidated `posIn` out of `hover_test.go` and `semantictokens_test.go` into a new file. The `pendingKeyword` latch fix in the same commit drew no advisory, and the difference is the usable filter: **a change is in scope when the task's own acceptance criteria cannot pass without it** (an automation body could not keep its list past a `command X` line), and out of scope when it merely fixes something noticed nearby, however defensible. A reviewer of the finished commit reads only the diff against the stated boundary, so the second kind lands as its own commit — before the feature when it is preparatory, after when it is cleanup.
- Apply when: an implementation task tempts you into a latent-bug fix, a helper consolidation or a rename outside its stated boundary or affected-file list.

## A lint warning fails `emod validate`, so a new rule sweeps every checked-in model before it lands
- Type: constraint
- Learning: `RunValidate` (`internal/cli/validate.go:39`) is `oracle.Check` — lexer, parser, validator *and* `linter.Lint` — and returns a `*LintError` for *any* diagnostic, severity ignored; `emod lint` behaves the same (`formatText` exits 1 whenever the list is non-empty, `formatJSON` exits 1 for warnings and 2 once an error is present). A warning-level rule is therefore not free: it turns every exit-0 assertion red for every model exhibiting the shape, which is why US-008 spent two of its three tasks moving models before the rule existed — Task 1 the three shared fixtures in `internal/test/fixtures.go` that hold `oracle.Check` clean-input leaves, Task 2 the checked-in `.emod` files (`examples/all_patterns.emod`, `internal/parser/testdata/all_patterns.emod`, `internal/parser/testdata/multi_context.emod`), each naming a view its own model already declares rather than inventing one. The models the repo ships as valid are now enumerated in one leaf, "returns no error for each model the repository ships as valid" (`internal/cli/validate_test.go`) — five paths, `examples/error_diagnostics_test.emod` deliberately not among them since it exists to produce diagnostics. Fixtures that must keep the flagged shape get no exemption: the `AutomationReads`/`TriggerReads`/`AutomationSchedule` kits moved out of oracle's "clean input" group into one named for what they now warn about, asserting the exact lines.
- Apply when: adding a rule to `internal/linter`, or adding an `.emod` file under `examples/` or `internal/parser/testdata/`.

## A lint fixture trips exactly one rule, so it is never the minimal model
- Type: convention
- Learning: each `const …Emod` in `internal/cli/lint_test.go` carries a comment naming precisely which rules it fires (`singleTagDCBEmod` "the only rule that produces info diagnostics", `automationWithoutViewEmod` "which automation/missing-todo-list reports and no other rule does") and its leaves assert `require.Len(t, entries, 1)`, so a fixture for a new rule has to keep the other fourteen quiet — `automationWithoutViewEmod` gives both its slices a full `flow` and its events real fields for that reason, since a command or event no flow names is `orphan-command`/`orphan-event`, and `left-chair`, `god-view`, `view-naming` and the `dcb/*` family are the other tripwires. Write the smallest model that shows the new shape and the count assertion fails on unrelated rules, which is exactly the point where a leaf gets weakened to `require.Contains` and stops proving the rule reports once.
- Apply when: writing the model fixture for a new rule in `internal/cli/lint_test.go` or `internal/linter/linter_test.go`.

## A rule whose message branches on model state is pinned by whole formatted lines
- Type: convention
- Learning: `automation/missing-todo-list` has two texts, chosen by whether the automation states `every`, and they share the prefix `automation %q reads no view, so ` and the suffix `; project a view of pending work and read it` — so `require.Contains` on either half passes against the wrong variant, and one on the rule name says nothing about which text was chosen. This is neither the shadowing case (a needle inside a needle asserted a line earlier) nor the short-keyword case (a needle inside unrelated words); it is two productions of one rule that only a whole-line comparison tells apart. Every leaf compares complete formatted diagnostics: `require.Equal` over `[]*diagnostic.Entry` literals (`internal/linter/linter_test.go`), over `reportedLines(diagnostics)` — one `d.String()` per entry, the helper `internal/validator/validator_test.go` already had and `internal/oracle/oracle_test.go` now carries too — and `require.Equal(t, path+":37: [rule] …", err.Error())` at the CLI layer. The list form pins order and count as well, so one leaf covers both slice homes and both texts.
- Apply when: asserting on a diagnostic whose wording varies with the model, or choosing between `Contains` and a whole-line `Equal` in a diagnostics test.

## A "no expected constant moves" criterion is unsatisfiable when the task edits a shared fixture
- Type: constraint
- Learning: US-008 Task 1 shipped with "`git diff` touches no golden, no `*FormattedEmod` constant and no transcribed name list", which cost a mid-run amendment (commit `e6970e1`) because the canonical constants *are* transcriptions of the fixtures: `keywordFieldFormattedEmod` (`internal/cli/fmt_test.go`) restates `test.KeywordFieldSearchCatalog` line for line, and the round-trip table's `activationEvents`/`triggerReads`/`automationReads` rows (`internal/formatter/formatter_test.go`) transcribe the names each fixture declares. Adding one `reads` line to a fixture necessarily moves both. Word the criterion as the amendment does — "the only expected values that move are those that restate a changed fixture's own text", naming each — so the guard still catches a golden in another package moving while staying satisfiable.
- Apply when: writing an acceptance criterion for a task that edits `internal/test/fixtures.go` or an `.emod` file a golden or transcribed list mirrors.

## Every editor-highlight surface now has an executable suite, and each is run a different way
- Type: pattern
- Learning: the three surfaces that decide what an editor paints are each pinned now, and they are wired differently. (a) `editors/tree-sitter-emod/queries/highlights.scm` is asserted by `editors/tree-sitter-emod/test/highlight/*.emod` — `tree-sitter test` picks these up automatically through the `highlights` entry in `tree-sitter.json`, so a new file there is a gate the moment it exists and needs no Taskfile or CI change. (b) `queries/folds.scm`, `indents.scm` and `textobjects.scm` are asserted by `editors/tree-sitter-emod/test/queries/*_test.go` — Go under `//go:build grammar`, driving the `tree-sitter query` CLI, run by `task test:grammar` (a plain `go test ./...` skips it, so it never requires the CLI). (c) `editors/vscode/syntaxes/emod.tmLanguage.json` is asserted by `editors/vscode/test/scopes/*.emod` through `vscode-tmgrammar-test`, run by `task test:vscode`. The last of the three needed adding to *both* the `test` aggregate in `Taskfile.yml` and a step in `.github/workflows/ci.yml` — a target in the Taskfile alone never runs on CI. This supersedes the older note that nothing cross-checks the TextMate alternation: it is checked now, and a scope change without a matching assertion file update fails.
- Apply when: adding or renaming a keyword, editing any `.scm` query, editing the VS Code TextMate grammar, or adding a test suite that `go test ./...` does not reach.

## A tree-sitter highlight marker only discriminates while another highlighted token follows it on the same line
- Type: constraint
- Learning: in `editors/tree-sitter-emod/test/highlight/*.emod`, a `# ^ capture` marker asserts against the nearest source line above — but if no further highlighted token follows the marked column on that line, the runner scans on into later lines and accepts a capture found there, so the assertion passes on anything. The guard is a trailing token on the same line: `constructs.emod` puts a trailing `# comment` after the description, the field line and the external system precisely for this, and both files' headers say so. Delete one of those trailing comments and its assertion silently stops discriminating while still reporting green.
- Apply when: adding an assertion to a `test/highlight/*.emod` file, or reviewing one that marks the last highlighted token on its line.

## `highlights.scm` field patterns select by anchor, never by `#match?` on the token's text
- Type: convention
- Learning: a field line's name, type and modifier are all `any_identifier`, so the three captures are distinguished by position, using leading `.` anchors — `(field_line . (any_identifier) @variable.member)`, then one more anchored `(any_identifier)` step per position. The previous `(#match? @type "^(string|date|timestamp|int)$")` form was wrong in both directions: it left a domain-typed field (`orderId RoomID`) uncoloured, and it could never colour a field typed `on`. Two traps sit in the anchored form. Unanchored, each pattern also matches every *later* identifier on the line, so one token collects up to three captures and the consumer's precedence rule — not this file — picks the winner. And an anchor skips only *anonymous* nodes, while a comment written mid-line parses as a named child of `field_line`, so every anchored step needs a `(comment)*` before it; the comment-split field line in `constructs.emod` is what holds that honest. The keyword list in the same file is hand-kept — nothing derives it from the grammar, and a keyword absent from it is simply uncoloured.
- Apply when: editing `editors/tree-sitter-emod/queries/highlights.scm`, especially anything touching `field_line` or the keyword alternation.

## A keyword that is only a keyword in one position never joins the flat TextMate alternation
- Type: constraint
- Learning: `repository.keywords` in `editors/vscode/syntaxes/emod.tmLanguage.json` is a positionless `\b(model|actor|…)\b` alternation, so anything added to it is coloured at *every* occurrence, including a field named after it. `on`, `every` and `type` therefore live in a separate `positional-keywords` rule keyed on their operand — `on` before an identifier (which takes the entity-name scope), `every` and `type` before a quoted string (which stays a string literal). Two further constraints ride along. That rule is deliberately case-sensitive where the surrounding rules use `(?i)`, because the lexer's keyword lookup is case-sensitive, so an event named `On`, `Every` or `Type` must keep its name scope. And a field line settles nothing on its own — `on RoomID` inside `fields` is the same text as an activation — so `fields` is a `begin`/`end` block rule whose inner `patterns` deliberately omit `#positional-keywords`; the shared `standalone-tokens` list exists so the file-level and block-level tails cannot drift apart. The other twenty-eight keywords keep the positionless treatment.
- Apply when: adding a keyword to the VS Code grammar, or explaining why a token is coloured inside a `fields` block when it should not be.

## A suite that pins another tool's output owes a mutated-input negative control
- Type: convention
- Learning: three tasks in one story each closed with a case that breaks a *copy* of the input and requires the run to fail, naming what it failed on — `queries_test.go` renames `trigger_definition` to `trigger_declaration` in a copy of `folds.scm` and requires the error to name both the file and the node type; `scope-assertions.test.js` mutates in both directions (rename `storage.type.emod` in a copy of the grammar, rename an expected scope in a copy of an assertion file) and additionally extends the flat keyword alternation with `on`/`every` to require the field-name assertions to fail on the *prohibited* scope. Without this, a query that compiles but captures nothing, or a grammar loaded from the wrong path, reports green and the suite proves nothing — which is the exact failure the suites were written to end. The mismatch helper asserts the position, the required scope and the scope actually produced, not just a non-zero exit.
- Apply when: writing or reviewing a test whose assertions come from an external tool's output (tree-sitter CLI, `vscode-tmgrammar-test`, any spawned formatter or linter).

## A test that shells out to a CLI runs with `-count=1`
- Type: constraint
- Learning: `task test:grammar` runs `go test -count=1 -tags grammar ./test/queries/...` because the inputs that decide the result — `grammar.js` and the `queries/*.scm` files — reach the test through the `tree-sitter` CLI, where the Go build cache cannot see them. Without `-count=1` a query file can be edited and the suite still reports a cached pass. The same holds for any test in this repo that reaches its subject through a spawned process rather than through Go imports.
- Apply when: adding a Taskfile target that runs Go tests which shell out to an external tool, or debugging a suite that stays green after its non-Go input changed.

## Untimed `exec.Command` in a test is the repo convention, and `go test`'s own timeout already bounds it
- Type: recurring-finding
- Learning: one review produced three findings (two `high`, one `medium`) against `editors/tree-sitter-emod/test/queries/treesitter_test.go` — no `context` on `tree-sitter query`, none on `tree-sitter generate`, unbounded `bytes.Buffer` capture — and execution disproved all three. The named triggers do not stall: every malformed-query and grammar-error shape returns in 20–215 ms because the CLI compiles the query before matching; the suite's own "node type the grammar does not define is rejected" leaf already proves the un-timed path returns an error. "Hang indefinitely" is unreachable regardless: `go test` injects `-test.timeout=10m0s` by default, nothing in `Taskfile.yml` or `.github/workflows` sets `-timeout 0`, and a manufactured stall produced a bounded `panic: test timed out` with a goroutine dump naming the exact line. `cmd.Stdin` left nil gets the child `os.DevNull`, closing the only genuine unbounded-wait vector. Buffer size is a build-time property of committed files (largest real capture 3.4 KB; the malformed path emits *zero* stdout bytes). Every other `exec.Command` in the repo is likewise untimed (`internal/cue/embed_test.go`, `internal/export/export_test.go`, `internal/cli/diagram.go`). The one real residual is a process leak on timeout — and `exec.CommandContext` would not fix it, since it kills the direct child, not the process group.
- Apply when: a reviewer raises a missing timeout, context or output-size bound on an `exec.Command` in this repo, or you are tempted to add `exec.CommandContext` to test-only tooling code.

## The viewer vitest pool is capped at two threads on purpose
- Type: constraint
- Learning: `internal/viewer/vitest.config.js` pins `pool: 'threads'` with `maxThreads: 2`. vitest otherwise runs one worker per core and gives each its own jsdom, so the 254 viewer tests reserve roughly a gigabyte on an eight-core machine — the entire memory cost of the repo's test suites, since the Go tests and the tree-sitter corpus together stay under a hundred megabytes. Measured: 1155 MB over 3 s becomes 367 MB over 4 s at two workers, and threads beat the default fork pool (386 MB against 514 MB at the same worker count). This matters because unattended runs execute the suite repeatedly and from several agents at once against one checkout. Do not restore the default, and give any new vitest project the same cap.
- Apply when: editing a `vitest.config.js`, adding a second JS test project, or diagnosing a machine crowded by parallel implementation worktrees.

## An ```emod fence is a promise that the block validates
- Type: convention
- Learning: `internal/oracle/oracle_test.go` "documented models" now extracts every fenced block from `README.md` and `docs/dsl-reference.md` and requires `oracle.Check` to report nothing. `emodBlocksIn` selects only lines that trim to exactly ` ```emod `, the origin passed to `Check` is `<document>:<line>` so a red run names one block rather than one document, and each document must yield at least one block so a broken extraction fails instead of passing over an empty list. Seven blocks are covered. Because `oracle.Check` is lexer + parser + validator + linter, a fenced block owes a whole model: every command referenced defined, every event produced consumed, views named `…View`, no orphan. The two blocks that are illustrations rather than models — the reference's identifiers pair, and the README's DCB sketch writing literal `...` placeholders — deliberately carry a plain ` ``` ` fence, and that is the only way to keep a fragment out of the harness.
- Apply when: adding or editing a fenced emod snippet in `README.md` or `docs/dsl-reference.md`.

## `examples/` is enumerated by its guard, and `_test.emod` means authored to fail
- Type: convention
- Learning: `internal/cli/validate_test.go` stopped naming example paths by hand. `examplePaths` reads `../../examples`, splits on the `_test.emod` suffix, requires both sides non-empty, and runs each file in a subtest named after it: every file without the suffix must return no error from `cli.RunValidate`, and every file with it must return one *and* have its expected diagnostics listed in the `demonstrated` map keyed by base name, or the leaf fails with "list the diagnostics %s is authored to demonstrate". The sibling leaf, previously "returns no error for each model the repository ships as valid" and naming two examples inline, now covers `internal/parser/testdata/*.emod` only. A new example is therefore guarded the moment it lands with no list to edit, and a deliberately broken one is guarded by asserting the diagnostics it demonstrates rather than by a skip entry that silently starts passing.
- Apply when: adding, repairing or deleting a file under `examples/`.

## The live proposals under `docs/proposals/ai/` are now the emod surface no test reaches
- Type: constraint
- Learning: with `README.md` and `docs/dsl-reference.md` covered by oracle's "documented models" leaf, the earlier learning that the reference is the one keyword surface no test reaches transfers to `docs/proposals/ai/`: seven of those files fence emod snippets and none is extracted or validated by anything. US-011's sweep had to rewrite six of them, and one hit (`08-docs-generation.md`) was in prose — "*automation* pattern: trigger event → command" — not inside a fence, so a fence-only search misses part of the surface. The historical documents in the same tree must *not* be swept: `docs/proposals/completed/`, `docs/proposals/triggers-and-automations-proposal.md`'s Before/removal-table sections, `user-stories/`, `tasks/completed/` and `tasks/learnings.md` record the world before a change and legitimately keep the retired spelling.
- Apply when: adding, renaming or retiring an emod keyword, or writing the sweep task for a story that changes a DSL spelling.

## `docs/dsl-reference.md` sub-heading anchors are cited more often than the numbered ones
- Type: constraint
- Learning: beside the number-prefixed `## <n>.` slugs the learnings already record, fourteen in-document links cite `###` sub-heading slugs — `#automation-pattern` ×6, `#command-pattern` ×3, `#translation-pattern` ×3, `#spec` ×3, `#view-pattern`, `#invariant` — against twenty `###` headings. Renaming `### Automation Pattern` to `### Trigger Pattern` while rewriting its body therefore breaks six links silently, and nothing in `Taskfile.yml` or `.github/workflows/ci.yml` checks markdown links. A task that rewrites the *body* of a pattern section must state that the heading text is held fixed, and the closing reconciliation lists both families — `^## [0-9]+\.` against `\(#[0-9]+-`, and `^### ` against `\(#[a-z]`.
- Apply when: editing, renaming or reordering any heading in `docs/dsl-reference.md`, especially a `### <Name> Pattern` one.

## A retirement sweep's "hits only in these files" criterion must whitelist the rejection tests
- Type: recurring-finding
- Learning: US-011 Task 5 could not close by the letter. Its criterion listed the prose files a retired spelling may still appear in and added "any hit in a `.go`, `.js` or grammar-corpus file means US-004 is incomplete" — but the retirement's own negative tests must spell the retired form verbatim: `internal/parser/parser_test.go` (six inline sources across the kinded-trigger rejection subtests), `internal/cli/validate_test.go:100` "returns error naming retired trigger kind replacement", and `editors/tree-sitter-emod/test/corpus/slice.txt` "Slice with trigger kind rejected". Satisfying the criterion literally would delete the guards a sibling criterion requires to hold. The same criterion's list of files *to change* was also short — it named five live proposals and the sweep found a sixth — because both halves were derived from one grep at decomposition time. Write the criterion against the behaviour ("no document presents the spelling as syntax to write today"), whitelist the negative tests, the grammar corpus error cases and the in-flight breakdown, and treat any enumerated file list as illustrative rather than exhaustive.
- Apply when: writing or closing a task whose acceptance criterion is a repository-wide search for a removed keyword, wire key or spelling.

## A guard that slices renderer.js source text by two `indexOf` markers inverts on a refactor
- Type: recurring-finding
- Learning: "uses no node fill or stroke literals outside the palette table in renderer.js" (`internal/viewer/tests/renderer.test.js:975`) scopes itself with `rendererSource.substring(rendererSource.indexOf('nodes.forEach'), rendererSource.indexOf('store.arrowData'))`. Extracting `buildBlock` and `appendEdge` above the node loop moved `store.arrowData` to offset 16438 and `nodes.forEach` to 17431, so start > end, `substring` silently swaps its bounds, and the 993-character window it now scans holds zero hex literals — injecting `svgRect(pos.x, pos.y, pos.w, pos.h, "#ff00ff", stroke, …)` into `buildBlock` still passes. Two independent reviewers raised it on one task. A marker-delimited source window is not self-checking: it must `expect` both markers found and start < end, and require a non-zero count of inspected literals, or the guard reports green on an empty region forever. Prefer the behavioural form the sibling leaf already uses — render `allElementTypes()` and compare `drawnBoxes(svg)` fills against `nodePalette` — because it survives any reordering. The same vacuity has a second shape in this suite: `expect(el.getAttribute('pointer-events')).toBeNull()` on the ctx-header rect, agg row rect and agg label pins the *absence* of an attribute no production path ever sets, so that half of "lets the pointer through the prose…" passes with the whole description feature deleted.
- Apply when: writing or reviewing a leaf in `internal/viewer/tests/` that reads a `static/*.js` file with `readFileSync`, or asserting that an attribute is absent.

## Text measurement is `layout.js`'s, and its font size and padding must not be restated
- Type: constraint
- Learning: `labelWidth` (`internal/viewer/static/layout.js:16`) is the viewer's only text measurer: it measures through a hidden `<text>` pinned at `font-size` 13 and returns the glyph width **plus a 16px pad**, both numbers private to the module. `renderer.js:11-12` re-declares them as `labelWidthFontSize`/`labelWidthPadding` so `labelAdvance` can rescale a width to another font size and `drawnWidth` can subtract the pad back off before truncating a header description — so measurement knowledge now lives in two modules and retuning layout's pad silently misplaces and mis-truncates every context and aggregate description, with layout's own tests green. When a second module needs a width, export the primitive from `layout.js` (an unpadded `textWidth(label, fontSize)` beside the padded `labelWidth`) rather than un-doing the padded one from outside.
- Apply when: fitting, truncating or positioning text in `internal/viewer/static/renderer.js` or `ui.js`, or changing `labelWidth`'s padding or measurement font size.

## The viewer has two tooltip mechanisms, and a marker glyph is its own hover target
- Type: constraint
- Learning: hover is served by an HTML overlay, not by SVG `<title>` — `ui.js` binds `pointerover`/`pointermove` and resolves `closest(HOVER_TARGETS)` where `HOVER_TARGETS = "[data-marker], .diagram-node"` (`ui.js:115`), then routes through `tooltipHtmlFor`. Three consequences an SVG-shaped mental model gets wrong. (1) A marker is matched *before* the block it sits in and a slice-header marker has no `.diagram-node` ancestor at all, so every marker must carry its own `data-node-id` as well as `data-marker` (`appendMarkings`, `renderer.js:305-316`) or it hovers dead. (2) A native `<title>` on anything that is also a hover target opens *beside* the HTML tooltip; the automation cadence `<title>` inserted as the group's first child (`appendBlockLabels`, `renderer.js:427`) is the only one in the file and the prose marks deliberately carry none. (3) `hoveredNode` returns null when the hovered id equals `store.interaction.selectedNodeId`, so a tooltip leaf that selects the node it then hovers asserts on an empty string. This narrows the earlier "the viewer shows a node twice" entry: a node value now has four readers — the drawn labels, the marker glyph, the HTML tooltip and the detail-panel row. The kinds of prose a mark can stand for are `PROSE_KINDS` in `static/config.js`, read by the renderer that writes the `data-marker` and by the tooltip that resolves it back.
- Apply when: surfacing a node value on hover, adding a glyph or badge to the canvas, or writing a leaf in `internal/viewer/tests/tooltip.test.js`.

## A row-placed block is a fraction of a stacked one, and no renderer fixture shows it
- Type: recurring-finding
- Learning: `placeRow` (`internal/viewer/static/layout.js:96-109`) splits the slice between the events, or the processors, it holds — `itemWidth = (sSliceWidth - 20 - (count-1) * L.sideGap) / count` — so a slice at its 220px minimum gives 94px at two items and 58.67px at three, against the 180px every `placeColumn` block gets. Anything drawn in a block's corner has to be measured against that width: an inset counted back from the right edge as a fixed distance crosses the block's midline and lands over its label. Every fixture in `internal/viewer/tests/renderer.test.js` puts at most one row-placed item per slice, so `placeRow` falls through to its `count > 1 ? … : sBoxWidth` else-branch and the suite only ever draws 180px boxes — a leaf meaning to exercise a narrow block needs three events (or three processors) in one slice, and can pin the width against a stacked sibling in the same render rather than against a constant. A block that narrow also cannot clear a centred label horizontally, the label spanning nearly the whole box, so what holds there is that the mark's ink and the label's ink do not overlap, not that the mark sits clear to the right of it.
- Apply when: drawing, insetting or moving anything inside a block in `internal/viewer/static/renderer.js`, or writing a leaf about where it sits.

## A comment rides a diagram node without its position, and only where a node exists
- Type: constraint
- Learning: `jsonDiagramNode.Comments` carries `convertComments(x.Comments)` for the actor, context, aggregate, both slice branches and every construct a slice declares — but the importer's `convertComments` (`internal/importer/importer.go:415`) copies `Text` only and drops the position on purpose: those positions point into the file the diagram was exported from, and a viewer save rewrites it line by line, so carrying them back anchors comments to stale lines. The second half is which constructs *have* no node: the model itself, an invariant, a spec, a flow and a `decides_on` clause, whose comments and descriptions cannot survive a viewer save at all — `ImportDiagram`'s doc comment names that set rather than claiming diagram JSON carries no comments. A round-trip receipt therefore needs a baseline stripped to exactly that set, and every leaf using it must first require the baseline still holds a construct comment, so an over-stripping helper cannot make the comparison vacuous.
- Apply when: carrying a new per-construct value through `internal/export/diagram.go` and `internal/importer/importer.go`, or writing the viewer-save round-trip receipt for one.

## `internal/viewer/static` is governed by the shared JavaScript naming guidelines
- Type: convention
- Learning: the JS under `internal/viewer/static` is reviewed against `~/.config/ai/guidelines/javascript/naming-patterns.md`, which is not auto-loaded with `CLAUDE.md` (only `comments.md` is referenced there) — so its rules get broken by default and then reported. The one that bites is factories: a function returning an object, an element or a markup string carries a `build*`/`create*` prefix (`buildTree`, `buildSVG`, `buildDescriptionHtml`, `buildFieldsHtml`, `buildTooltipHtml`), so a new HTML-returning helper named `somethingHtml()` draws a finding; verb-noun (`applyViewport`, `commitDrag`) is for operations, and private helpers take no `_` prefix. Read the file before adding functions to `ui.js`, `renderer.js`, `layout.js` or `interaction.js`.
- Apply when: adding or renaming a function in `internal/viewer/static/*.js`.

## A container's id is carried by more elements than any class list matches
- Type: recurring-finding
- Learning: a context's chrome is two `rect.ctx-header`s (rounded and square, `renderer.js:255,257`) plus `text.ctx-label`; an aggregate's is `rect.agg-row` (`renderer.js:283`), `rect.agg-area` (`renderer.js:301`) and `text.agg-label` — and every one of them carries `data-ctx-id`/`data-agg-id`, as does any mark `appendHeaderLabels` paints on the band, which takes `style.ownerAttr` and deliberately wears none of the band's classes because those drive highlight-on-click and the pan suppression `closest('.arrow-hit, .ctx-label, .agg-label')` in `interaction.js:533,790`. `closest("[data-agg-id]")` is therefore a strict superset of the `.agg-area, .agg-label` pair a class-matching handler reaches: rewriting the contextmenu delegation to the attribute form widened the right-click target to `rect.agg-row`, which had fallen through to the browser's native menu, and a reviewer raised the widening as unrequested behaviour on a task whose file list did not name it. Enumerate the id-carrying elements before trading a class chain for an attribute lookup, and state in the commit which targets the trade adds.
- Apply when: editing an event-delegation branch in `internal/viewer/static/ui.js` or `interaction.js`, or adding a new element to a context or aggregate header.

## A container has no detail panel, so its header is the only place its prose can be read
- Type: constraint
- Learning: `showDetailPanel` has exactly one caller — `viewer.js:248`, the `open-field-editor` context-menu item, resolving `menu.targetNodeId`. A context's and an aggregate's menu carries `aggOrCtxId`/`targetAggId` and offers no such item, so neither container opens a panel at all. Every value those two carry has to be readable off the swimlane header or the aggregate label row itself: the description is drawn along the band, and comments are reachable only through the hover mark in the header. This bounds the "the viewer shows a node twice" entry — a container is shown once, and a plan that plants a new container value in the detail panel has nowhere to put it.
- Apply when: planning or reviewing a task that surfaces a new per-construct value in the viewer, and deciding where a context's or aggregate's value can be read.

## Which marks a surface draws is a per-surface list, not `PROSE_KINDS`
- Type: constraint
- Learning: `appendMarkings(parent, node, band, fill, attrs, kinds)` filters `kinds || PROSE_KINDS` (`renderer.js:328,347`). Blocks and slice headers pass nothing and get every kind; the two container headers pass `headerMarkingKinds = ["comments"]` (`renderer.js:21`), because a container's description is already written along its band and a mark for it would open a tooltip repeating what is on show. Adding a third entry to `PROSE_KINDS` in `config.js` therefore turns it on for blocks and slices silently while leaving headers untouched — both halves want a deliberate decision and a leaf. `markingsLeftEdge` (`renderer.js:338`), which is what makes a header's name and description give way rather than run under the marks, takes the same `kinds` argument and must be handed the identical list as the `appendMarkings` call beside it, or the text yields to a mark that is never drawn.
- Apply when: adding a prose kind to `internal/viewer/static/config.js`, or drawing marks on a new surface in `renderer.js`.

## A leaf about how two drawn things relate must first pin that they overlap
- Type: recurring-finding
- Learning: renderer.test.js "lets the pointer through the prose to the header and row it is painted over" read `pointer-events` off the prose and off the bands beneath, but nothing required the prose to be over a band: a description moved 500px below its band passed the whole leaf. The fix is the shared `insideBand(band, el)` (`internal/viewer/tests/renderer.test.js:441`), which every spatial claim in the suite now asserts before reading its consequence — containment by `insideBand`, clearance by `inkOfCentred(mark)` against `rightEdgeOf(text)`, and `withoutMarks(...)` (line 454) to keep a mark out of a name/description pair. A "does not overlap" claim is stated ink-against-ink rather than as an ordering of anchor points, because a row-placed block's centred label spans nearly the whole box. This is the third shape of the same vacuity in this suite, after the `pointer-events` absence and the `indexOf`-windowed source guard: a consequence asserted over a premise that never held.
- Apply when: writing or reviewing a leaf in `internal/viewer/tests/renderer.test.js` claiming one drawn element sits over, inside, or clear of another.

## "Adds nothing but the mark" is four snapshot families across a plain and a featured render
- Type: pattern
- Learning: the viewer proves a new piece of canvas ink disturbs nothing by rendering one fixture twice — plain, then with the feature's field set — and requiring equality of four helpers in `internal/viewer/tests/renderer.test.js`: `drawnFrames` (line 568: every `rect.ctx-header/.agg-row/.agg-area/.slice-box/.slice-header` as class plus x/y/w/h), `namePlacements` (line 576: `.ctx-label, .agg-label`), `drawnBoxes` (line 552: each `.diagram-node`'s rect including fill) and `placements(proseIn(svg))` (line 502). Each covers what the others miss — frames alone let text shift, box positions alone let the swimlane reflow, and none of the first three sees a description that moved. Reuse all four rather than hand-rolling a narrower comparison, and add a family when a new kind of drawn thing appears.
- Apply when: adding a glyph, badge or other ornament in `internal/viewer/static/renderer.js` and writing the receipt that nothing else moved.

## A truncation budget is spent in the measurer's font size, not the drawn one
- Type: constraint
- Learning: `drawnWidth` measures through `Layout.labelWidth`, which is pinned at font-size 13, so a budget expressed in drawn pixels cannot be compared with it directly when the string is painted at another size — a 16px context name fitted against a raw pixel budget cuts too late and overruns whatever the budget was protecting. `fitWithEllipsis(text, maxWidth, fontSize)` (`renderer.js:62`) converts first, `room = maxWidth * labelWidthFontSize / fontSize`, and `labelAdvance(label, fontSize)` (line 50) does the inverse for placement. Pass the drawing font size to both whenever the text is not 13px; `ctxHeaderStyle.nameSize` is 16 and is the one that bites, while `aggHeaderStyle.nameSize` at 13 matches the measurer and hides the bug.
- Apply when: fitting, truncating or advancing past text in `internal/viewer/static/renderer.js`.

## A recovery drain must discard the unit the grammar admits, not the line it sits on
- Type: constraint
- Learning: `skipRestOfLineOrBlockEnd(tok)` fits a declaration that cannot span lines; a flow entry can, so draining a line leaves the continuation behind and the block loop reports it as a spurious second diagnostic. Two narrower fixes both failed on inputs their author had not enumerated — draining the offending token's own line eats the *next* entry when the malformed one was truncated, and special-casing "leave it alone if it is a `command` keyword" covers only the subset where the offending token itself wrapped. What works is `skipToNextFlowEntry` (`internal/parser/parser.go`), which advances to whatever starts the next entry: the opening keyword, the block's `}`, or EOF. The keyword is an unambiguous resume point only because no entry can contain it anywhere else, which is why the rejection entry's invariant slot excludes `command` while accepting every other identifier-like token. The receipt that a drain is right is not a handful of hand-picked shapes: it is every malformed shape crossed with every place the construct can wrap, which took the emod flow block from six cascading combinations to none.
- Apply when: writing or changing error recovery for a block entry in `internal/parser/parser.go`, or asserting `require.Len(t, diags, 1)` for a construct whose parts may legally sit on more than one line.

## Tightening a parser to close a hazard is a compatibility change, and the grammar is the other half
- Type: recurring-finding
- Learning: gating every token of a flow entry on the opening keyword's line closed a real hole — a truncated entry silently reading the next line's identifier as its own — and simultaneously rejected `command -> event:` entries wrapped across lines that the parser had always accepted, breaking the story's own "every existing `.emod` file stays valid with unchanged meaning". No checked-in model wrapped one, so nothing in the suite noticed. The same run shipped the mirror-image defect in the other direction: `flow_rejection_entry` in `editors/tree-sitter-emod/grammar.js` took `$.identifier` where `invariant` and `spec_then` take `$.any_identifier`, so the editor red-squiggled the lowercase `<invariantName>` the new DSL reference itself uses. Both are the same omission — changing what one side accepts without re-deriving what the other does. Before narrowing an accept-check, name the input that stops parsing and find it in `examples/`, `internal/parser/testdata/` and the fixtures; before widening the grammar, check the Go parser actually accepts what you just admitted.
- Apply when: adding or tightening a `p.check`/`checkSameLineAs` guard in `internal/parser/parser.go`, or changing which rule a slot in `grammar.js` takes.

## A leaf that pairs the feature with something independently satisfying the same rule cannot see the feature
- Type: recurring-finding
- Learning: the leaves for "a rejection edge counts as a reference for `spec/invariant-never-exercised`" each paired the edge with a spec exercising it — added to keep the sibling rule `flow/rejection-without-spec` quiet. That spec is itself a reference, so it silenced the rule on its own and six of seven leaves passed with the production change reverted. The tell is structural, not statistical: the fixture contains a second thing sufficient for the assertion's outcome. The fix is to make the feature the *only* sufficient cause and filter the other rule's diagnostics out by name (`linesReportedBy(diags, rule)`), not to quieten the other rule by adding to the model. This is the same shape as the `NotEqual`-on-a-stripped-twin and `assertHover`-returns-what-it-pinned entries above, reached from a third direction: silencing unrelated noise by enriching the fixture.
- Apply when: writing a leaf whose assertion is an absence, over a fixture built to keep some other rule or check quiet.

## The audit's own fixes need auditing; the second round is where the regression shows up
- Type: pattern
- Learning: the first adversarial pass over US-009 returned 22 confirmed defects. Fixing them introduced a new one — removing the over-strict line gate left the recovery drain anchored to a line it no longer matched, so a wrapped malformed entry reported twice where the base reported once — and the second pass caught exactly that, plus five more. A third check, done by hand as an exhaustive sweep rather than another fan-out, found six further combinations the second round's fix still missed. The rule this repo should carry: a fix that changes a code path is new work and gets the same scrutiny as the original, and the cheapest form of that scrutiny for a recovery or parsing change is enumerating the input space (every shape × every layout) rather than adding one leaf per finding. Budget for it — two review rounds plus a sweep, not one. US-011 needed three, and the reason generalises: when a defect is "this name was resolved at the wrong scope", the fix corrects the *one* lookup the audit reproduced and leaves its siblings, so round three found in the `when` command exactly what round two found in the `given` event. On a scope or resolution finding, fix every lookup in the function at once and sweep the space before re-auditing, or each round buys one row of a table you could have written out in ten minutes.
- Apply when: applying review findings that touch a code path rather than a comment, a name or a redundant test — especially a finding about how a name is resolved.

## A recovery drain that skips to the block's end deletes the entries below the bad one
- Type: recurring-finding
- Learning: US-010's first audit round found `parseSpecElementsUntilListEnd` erroring per token when an unclosed payload left it scanning the construct below (8 diagnostics for one typo). The fix — drain to `atSpecEventListEnd` and `break` — traded a cascade for silent data loss: `given [A, "oops", B]` reported once and dropped `B` from the AST entirely, and because `oracle.Check` validates the recovered model regardless of parse diagnostics, every semantic error on the dropped elements vanished with it. The second round caught exactly that. What works is neither extreme: latch a `reported bool`, report the first offending token only, then `advance()` and keep reading, so the list still yields every valid element it holds. This is the third recovery shape this repo has got wrong in the same place, after `parseIdentifiersUntil`'s terminator set and `skipToNextFlowEntry`.
- Apply when: writing or fixing error recovery for a bracketed list or block body in `internal/parser/parser.go`, especially when reducing a diagnostic cascade.

## An unclosed inner block takes the enclosing block's brace, and no local signal separates them
- Type: constraint
- Learning: every block body in this parser loops `for !p.check(lexer.CloseBrace)` and consumes the first `}` it meets, so an unclosed `fields {`, `tags {`, `spec {` or payload `{` steals the brace of whatever encloses it. For a payload the ambiguity is total: its `}` and the spec's are the same token, indentation is not read, and the closing brace of a well-formed multi-line payload also sits on a later line at a shallower column than its opening one — so line and column separate nothing. What *can* bound it is the enclosing construct's own terminator: `atPayloadEnd` stops at `]` (the spec list's bracket) and at a bare `given`/`when`/`then` (a sibling entry), which covers a payload written anywhere but last. A criterion phrased "an unclosed X reports exactly one diagnostic and the enclosing blocks all close" is therefore satisfiable only for the shapes something follows; write it naming those shapes, or it ships ticked and untrue.
- Apply when: adding a braced sub-block to a construct in `internal/parser/parser.go`, or writing an acceptance criterion about unclosed-brace recovery.

## A number literal reaches the exports as digits, never as a float64
- Type: constraint
- Learning: `internal/export` writes payload numbers through `canonicalNumber` into `json.Number` and straight into CUE. Both halves are load-bearing and each was a shipped defect the audit reproduced. Parsing to `float64` loses precision silently — `99999999999999999999` exported as `1e+20` — and it is reachable because a domain-typed field accepts any literal unchecked. Writing the source text verbatim is worse: `007` is a legal emod integer and an illegal JSON *and* CUE one, so `cue vet` rejected the whole exported document with "illegal integer number", schema included. Trimming leading zeros and carrying the remaining digits satisfies both formats and keeps `12.50` whole. Note `emod` and CUE disagree on strings in the opposite direction: emod has no escape sequences and CUE does, so `internal/formatter`'s `quoted` must *not* escape and `internal/export`'s `cueLiteral` must (`%q`) — the same value, two correct spellings.
- Apply when: carrying a literal value into `internal/export/json.go` or `cue.go`, or reviewing a change that writes a source-text number into a serialized document.

## A shared index keyed by construct name alone pools two kinds that may share one
- Type: recurring-finding
- Learning: US-010's payload check filed every command's and event's fields in one `map[string]map[string]*ast.Field` keyed by name, so a model declaring both `command Borrow` and `event Borrow` accepted on either a payload field only the other declares, and the diagnostic named the wrong kind. Nothing in the suite noticed: the shared fixtures give commands and events distinct names, which is also why `declaresCommandOrEvent` has always looked safe. A spec's `when` resolving against commands *and* events is exactly the shape that makes the collision reachable. Key such an index by kind — `commandFieldsByName` beside `eventFieldsByName` — and resolve a reference to a small `{kind, fields}` value so the lookup and the diagnostic's wording cannot disagree.
- Apply when: adding a per-construct index to `internal/validator`, or reasoning about a spec reference that resolves against more than one construct kind.

## A criterion can ask for something the language cannot express
- Type: constraint
- Learning: US-010 Task 5 required a payload value containing a double quote to survive parse → format → parse. An emod string runs verbatim from one quote to the next, so no source can hold one — the criterion was unsatisfiable as written, and the pre-existing description hazard table omits the double quote for the same reason. The behaviour it was reaching for is still testable, but only from the AST: a model built in Go (as the importer and the viewer's save path build one) does carry such a value, and `internal/export`'s CUE table exercises it there. Two lessons: check a round-trip criterion against what the *language* admits before ticking it, and remember that a writer's inputs are not limited to what a parser can produce.
- Apply when: writing or closing a round-trip criterion over string content, or choosing hazard characters for a formatter or exporter test.

## A tag key is an indirection: the event's field is `tags`, never the predicate's `Value`
- Type: recurring-finding
- Learning: `where tag(desk = deskId)` records `Field: "desk"` (the tag key) and `Value: "deskId"`, and `predicateDiagnostics` (`internal/validator/validator.go`) checks `Field` against `index.eventTagKeys` and `Value` against `index.eventFields`. Reading that as "`Value` is the tagged field" is wrong on both sides and US-011's decomposition made exactly that call as a decided open question. `Value` names the *command's* field the query compares against; the *event's* side is whatever its own `tags { key: fieldRef }` block binds the key to, which is the entire reason the mapping has a syntax. Every model in the tree writes `tags { desk: deskId }` against `tag(desk = deskId)`, so the two coincide everywhere and nothing catches the confusion — an adversarial round reproduced both a false positive (a model whose bound field agrees, warned about on a same-named field the query never reads) and a false negative (the bound field genuinely disagreeing, silent). The validator cannot catch it either: nothing cross-checks that a predicate's field ref is the field its tag key maps to, so the divergent model validates clean. Resolve `Field` through the event's `Tags` and treat an event that binds no such key as no basis for comparison.
- Apply when: reading `ast.TagPredicate` anywhere — a lint rule, an exporter, a diagram, the LSP — or writing a decomposition note about what a `decides_on` predicate names.

## A linter index keyed by construct name must be scoped to the context
- Type: recurring-finding
- Learning: `checkGivenOutsideBoundary` built its command index from `model.AllSlices()` with `index[cmd.Name] = cmd` — last declaration wins — and `eventHomeIndex` answers with `homes[0]`, the first. Construct names are only unique within a context, so both are coin flips on a model where two contexts declare one name, and US-011 shipped both halves of that mistake one audit round apart: the event lookup read a tag mapping off another context's event, and the command lookup compared one context's payload against another context's predicate, with declaration order alone flipping the diagnostic. A model-wide index is right only for a model-wide question — `eventHomeLookup` deciding "is this a declared event at all" is fine. Anything that then reads *structure* off the resolved construct (its tags, its predicate, its fields) must resolve within the enclosing context, through one `ctx.AllSlices()` walk that covers both slice homes and translation-declared events. Note the two-sidedness: a lookup like this usually has a sibling, and fixing one leaves the other.
- Apply when: adding a rule to `internal/linter` that resolves a spec's `when`, a `given`, or any construct by name, or reviewing an index built from `model.AllSlices()`.

## A decided open question derived from a half-constraining validator check is the one to distrust
- Type: recurring-finding
- Learning: US-011's decomposition settled "which half of a `TagPredicate` is the tagged field" by reading `predicateDiagnostics`, and got it wrong — because that check constrains `Field` and `Value` *separately*, against tag keys and against event fields, and never relates them. A validator that half-constrains a relationship reads, to a planner looking for ground truth, exactly like one that fully defines it. The decomposition's own blast-radius measurement then inherited the error and reported a count that a later audit falsified. Two habits follow. When a decomposition cites a validator check as settling a semantic question, check whether that check relates the parts or merely admits each one; and when an audit contradicts a decided open question, correct the code and record the correction here rather than editing the criteria to match — the criteria were approved, and a breakdown archived with a decision the code contradicts is worse than one whose correction is written down beside it.
- Apply when: writing or reading a decomposition's "open questions, decided" section, or closing a task whose implementation contradicts one.

## A slice's own events and its translations' nested events are two fields, so collection order is not source order
- Type: constraint
- Learning: `slice.Events` and `slice.Translations[].Event` are separate AST fields, so a translation written *above* a sibling event is collected *after* it. US-012's duplicate-wire-type check walked them in collection order and reported the collision on whichever of the pair the AST yielded second, crediting the other as the prior binder — the reverse of the file for any model whose translation comes first (verified: the error landed on line 17's nested binding and named the line-26 sibling as the earlier one). The fix is to sort the *inputs* by the position the check reports at, not to sort the emitted diagnostics: `sortInDeclarationOrder(diags)` reorders the list while leaving the attribution wrong, so a check that names an *earlier* element ("already bound by X", "first declared at Y") cannot be repaired downstream. This narrows the existing "diagnostics gathered from more than one AST collection must be position-sorted" entry: sorting the output is sufficient only when each finding is self-contained.
- Apply when: writing any validator or linter check that walks both a slice's own constructs and those a translation nests, or whose message names a construct other than the one it is positioned at.

## An ordering guard needs a fixture the AST does not already yield in order, and must pin attribution
- Type: recurring-finding
- Learning: two rounds of audit killed two versions of the same guard. First, a "comes back in declaration order" leaf whose fixture spanned two contexts could not fail — `Model.SliceRefs` walks contexts in declaration order and `Context.SliceRefs` already sorts each context's slices by position, so the entries were ascending before any sort ran; deleting the sort left the whole repo green. Second, the replacement interleaved a translation-nested binding between a slice's own events but still put the earliest binding in the slice's own list, so both implementations agreed on who bound first and only the line order differed — restoring the defective shape passed. What discriminates is a fixture whose *earliest* element is the one collection order yields *last*, asserted as whole formatted lines so the prior-binder name is pinned alongside the position.
- Apply when: writing the receipt for a check that sorts, or reviewing a leaf whose name claims order.

## A TextMate positional rule's qualifiers are pinned by prohibitions, never by its positive assertion
- Type: convention
- Learning: `\btype(?=\s+")` in `editors/vscode/syntaxes/emod.tmLanguage.json` carries two qualifiers a positive assertion cannot see. Asserting that `type` *is* `keyword.control.emod` before a quoted string still passes when the rule is broadened to `\btype\b` or made case-insensitive with `(?i)` — both mutations left all nine `test/scopes/*.emod` green. Each qualifier needs a case that must *not* paint: a tag key (`tags { type: deskId }`) for the lookahead, and a capitalised name that *is* followed by a quoted string (`invariant Type "…"`) for the case-sensitivity. Both then fail reporting `prohibited scopes: keyword.control.emod`. Note the asymmetry with tree-sitter, where anonymous keyword literals are exact-match: no grammar edit can make `Type` read as the `type` keyword, so the same case-sensitivity assertion is filler there and load-bearing here. Do not mirror the two surfaces' assertions.
- Apply when: adding a keyword to `#positional-keywords`, or writing case-sensitivity assertions across both editor surfaces.

## `clerk verify`'s dead-code check false-positives on every symbol in `internal/test`
- Type: constraint
- Learning: the check reports "defined but referenced nowhere outside its own file and the tests", which is the defining property of a fixture package — `internal/test` exists to be consumed by `_test.go` files and nothing else. US-012's `WireTypeLibraryLendingModel`, `WithoutWireTypes` and `DeclaredWireTypes` were all blocked despite having three and four cross-package consumers each, and every pre-existing kit member (`SpecLibraryLendingModel`, `WithoutSpecs`, `DeclaredSpecNames`, `DeclaredAutomationReads`) has the identical shape. The six-part fixture kit the repo mandates cannot satisfy this check by construction. Confirm the consumers with `rg -l 'test\.<Symbol>\b' --type go` and record the judgement; do not delete a live fixture to open the gate.
- Apply when: `clerk verify` blocks on dead code for a symbol declared in `internal/test`.

## Renaming a TextMate repository key drifts the unstarted breakdowns that cite it
- Type: constraint
- Learning: `tasks/us-013-*.md` and `tasks/us-017-*.md` name the VS Code grammar's `#activations` key and its `:67-84` line range inside acceptance criteria and codebase-context sections, and US-017 carries a criterion asserting that rule is unchanged. US-012 renamed the key to `#positional-keywords` — sanctioned by its own criterion, since the group had grown past activations — and silently invalidated those references; nothing links a breakdown to the file it cites. The assertion *file* `test/scopes/activations.emod` was not renamed, so `.json` affected-file lists survived and only the prose drifted. Before renaming a repository key, a query name or a rule group, grep `tasks/*.md` for it and either hold the name or report the drift; a planner reads those files as ground truth long after the rename.
- Apply when: renaming a key, group or rule in a file that unstarted breakdowns describe, or reconciling a breakdown against a tree that moved under it.

## The exhaustive keyword corpus case must gain every new keyword, not a dedicated case beside it
- Type: convention
- Learning: `editors/tree-sitter-emod/test/corpus/fields.txt` holds "Field named after each keyword inside a fields block", which lists every spelling `lexer.Keywords()` reports. US-012 left `type` out of it — to avoid touching an existing expectation — and added a dedicated single-keyword case in `wire_type.txt` instead, which made `type` the only keyword the exhaustive case omitted and its stated scope false. Extending the list is one source line plus one `(field_line (any_identifier) x3)` triple, and the source-line count and the node count must move together. A dedicated case is right only for a *position* the exhaustive one does not cover, such as a field *typed* after the keyword.
- Apply when: adding a keyword to `internal/lexer/token.go` and writing its tree-sitter corpus coverage.

## A set that doubles as a rule's adoption guard needs the emptiness check mutated, not the collection arm
- Type: recurring-finding
- Observed: US-001 task 2 — flagging a read model nothing reads
- Learning: `view/never-read` builds one model-wide `readViews` set and treats `len(readViews) == 0` as "the model has not adopted `reads`", the same shape `spec/command-without-spec` uses for `hasSpec`. Mutation-testing it by disabling a whole collection arm (`if false && slice.Trigger.Reads != ""`) fails loudly and proves nothing about the arm's `!= ""` guard: dropping *only* the emptiness check adds `""` to the set, which leaves the set non-empty, opens the adoption guard, and reports every view of a half-drafted model — and it left the entire repository green. The two mutations are independent and only the second one witnesses that an omitted `reads` is a supported state (`checkMissingTodoList` exists for exactly that state). Run both, and give the omitted-entry case its own leaf filtered by rule name (`linesReportedBy(diags, rule)`), since an automation stating no `reads` legitimately fires the sibling rule.
- Apply when: adding a lint rule whose guard is "the model states at least one X", or mutation-testing any set that is both a lookup and a has-adopted-the-concept flag.

## A silence assertion under a guarded rule must prove the guard is not what silenced it
- Type: recurring-finding
- Observed: US-001 task 2 — flagging a read model nothing reads
- Learning: for a rule that stays silent until the model adopts a concept, `require.Empty(t, diags)` has two sufficient causes — the consumer was found, or the guard never opened — and a fixture that states its only `reads` on the construct under test hands you the second. The first version of "a reads on a dcb context's own slice counts as a consumer" did exactly that: narrowing the `readViews` walk to aggregate homes left the set empty, the guard suppressed everything, and the leaf passed under the mutation it was written to catch. The fix is a second `reads` elsewhere in the model — on a view that is itself read, so it adds no diagnostic — which keeps the set non-empty whatever the walk covers and makes the construct under test the only thing that can silence the view. This is the guard-shaped instance of "A leaf that pairs the feature with something independently satisfying the same rule cannot see the feature": there the second sufficient cause was another rule, here it is the rule's own guard.
- Apply when: writing a "no diagnostic" leaf for a rule that has an adoption guard, or reviewing one whose model states a single `reads`, spec or tag.

## Never run `audit-implement` against the checkout you are working in
- Type: constraint
- Observed: US-001 phase 3 — flagging a read model nothing reads
- Learning: the audit's verifiers prove claims by mutating the working tree, and they do not always clean up: the first round reported the shared checkout being concurrently mutated and one mutation passing spuriously because of it, and stopping the second round mid-flight left a **staged** wholesale revert of task 1 behind — `git status --porcelain` showing `M ` in the first column, three files, the exact inverse of a landed commit including doc comments added minutes earlier as an audit fix. That state fails seven leaves, so integrating it would have put a red tree on `main`. Two rules follow. Check `git status --porcelain` after every audit round and *before* trusting any command against the tree, treating a staged inverse of your own commit as residue rather than intent. And prefer running the audit from a throwaway `git worktree` at the head SHA, which is what its own verifiers fall back to when they need an uncontended checkout.
- Apply when: invoking `audit-implement`, stopping a workflow mid-flight, or finding unexplained staged changes after a background run.

## A criterion may name an artefact the repository does not have
- Type: constraint
- Observed: US-001 task 3 — flagging a read model nothing reads
- Learning: the story asked that the rule "appears in the reference's lint listing", and `docs/dsl-reference.md` has no lint listing — no rule table, no rules section. Rules reach the reference as a bullet inside the section owning the construct (`spec/invariant-never-exercised` under `### invariant`, `flow/rejection-without-spec` under `## 7. Flows`, `wire/type-format` under `### Wire Types`), and `### View Pattern` carried no bullet list at all, so `view/never-read` is that section's first. The criterion was satisfiable only by reading it as intent rather than as a literal location. A planner writing a criterion that points at a named place in a document owes one `rg` confirming the place exists; a criterion naming a container that has to be invented is a design decision smuggled in as a checkbox, and the author discovers it with the file already open.
- Apply when: writing an acceptance criterion that says a change "appears in" a table, listing, index or section of a document, or closing one whose named location you cannot find.

## Repairing a model to satisfy a new check is a modelling decision, not a rename
- Type: recurring-finding
- Observed: US-002 audit rounds 1 and 2 — resolving a trigger's and a translation's `reads`
- Learning: when a new check turns a checked-in model into a reporting one, the repair looks mechanical — point the dangling name at something that resolves — and it is not. Round 1 flagged that pointing `all_patterns.emod`'s translation at `AvailableRoomsView`, the name its trigger already read, left the fixture unable to tell the two `reads` entries apart: a stub emitting that constant passed both the formatter round-trip and the parser integration suite. The obvious repair — declare the `BookingComWebhookView` the translation had been reading — was measured as cheap (one expected value moved, whole suite green) and taken, and round 2 upheld two mediums against it: the only event available to feed that view is `ExternalReservationImported`, which the very translation reading it emits, so the flagship example gained a cycle and a read model fed by its own consumer, inverting the External System → View → Command → Event shape `docs/dsl-reference.md` documents. Cost of test churn is the wrong axis to measure a fixture repair on; ask what the model now claims about the domain. Both corpora declare fewer views than they state `reads`, so a collision there is unavoidable and is the better trade — an honest inbound view needs an event carrying `source external`, which no example declares.
- Apply when: repairing a fixture, example or testdata model so it satisfies a newly-widened validator or lint check, or judging a "the fix is cheap, the suite is green" argument for editing a checked-in model.

## A model-wide flag needs a two-context leaf, not just a two-home one
- Type: recurring-finding
- Observed: US-002 task 5 — silencing `view/never-read` on an unresolved `reads`
- Learning: "a slice has two homes" and "resolution is model-wide" are two different claims and each needs its own fixture. `readsUnresolved` was pinned first for neither: every model in the `view/never-read` group is built by `lendingWith` (`internal/linter/linter_test.go`), one context holding one aggregate, so both an aggregate-only collection walk *and* a suppression scoped per context passed the whole suite. The two mutations are independent and so are their witnesses — a leaf whose view is declared on a `mode dcb` context's own slice kills the first, and a leaf whose unresolvable `reads` sits in one context while the unread view sits in another kills the second. Note the trap that hid this: a mutation introducing a per-context flag leaves the model-wide variable unused, so `go test` fails on a *compile* error with no `--- FAIL` line, which reads exactly like a caught mutation. Add `_ = <unusedVar>` and re-run before believing a mutation was caught.
- Apply when: adding a model-wide fact to `internal/linter`'s `Lint`, or mutation-testing anything built by a single-context fixture helper.

## `clerk` resolves the wrong breakdown when `tasks/` holds several
- Type: constraint
- Observed: US-002 phase 3 — landing the run
- Learning: `next`, `status`, `finish`, `gate`, `land`, `story` and `sidecar` all take `--tasks-file`, and `clerk --help` says to pass it when `tasks/` holds more than one breakdown — which this repo always does (seven at the time of writing). Omitting it does not error: `clerk land --integrate` merged and deleted the branch correctly but returned `"archived": null`, silently leaving the breakdown in `tasks/`, and `clerk verify` reported a commit-boundary warning about a "task 7" belonging to a different story entirely. Pass `--tasks-file` on every clerk call of this run, and read `archived` in `land`'s output rather than assuming the move happened. Note also that archiving after integration puts the archive commit on the default branch instead of riding with the work, which stales the receipt — re-run the suite and re-record.
- Apply when: invoking any `clerk` subcommand in a repo whose `tasks/` holds more than one breakdown, or explaining an unexpected task number in a `clerk verify` finding.

## `noclobber` makes a scratchpad redirect fail while the command still runs
- Type: constraint
- Observed: US-002 phase 3 — capturing suite output for a receipt
- Learning: this session's zsh sets `noclobber`, so `cmd > /path/that/exists` is refused with `file exists:` and the shell returns 1 — while the pipeline's own exit status is lost. `mise exec -- task test > fullsuite.txt` reported `exit=1` with a green suite, which reads exactly like a failing test run, and a following `awk`/`grep` happily reads the *stale* file as though it were fresh output. Three commit agents hit the same trap on one run. Use `>|` to force, or a filename unique to the step. This is the same family as the `cp`/`mv`/`rm` prezto `-i` aliases already recorded under file operations.
- Apply when: redirecting command output to a scratchpad path in this session, or diagnosing an exit code that disagrees with the output it captured.

## `git stash pop` on a clean tree pops somebody else's stash
- Type: constraint
- Observed: US-013 phase 3 — comparing a render against the base commit
- Learning: `git stash -u` saves nothing when the tree is already clean and exits 0 with "No local changes to save", so a later `git stash pop` in the same command targets whatever entry was already at `stash@{0}` — which in this repo is another session's `autostash`, since every worktree of one repo shares a stash stack. The pop conflicted in `tasks/learnings.md` and left `UU` in the index; only the conflict saved it, because a clean pop would have silently applied another run's work and then dropped the entry. Never pair `stash`/`stash pop` around a temporary checkout: use `git worktree add --detach <sha>` and build there instead. Two riders on that. The throwaway worktree cannot run `cmd/emod` until `internal/viewer/generated/` exists, because `embed.go` has `//go:embed generated/*` and those artefacts are gitignored — copy them in from the main checkout. And a before/after byte comparison must assert each render *succeeded*: comparing two CLI usage errors reports IDENTICAL and proves nothing, which is exactly what a wrong flag name produced here.
- Apply when: rendering or building at a second commit to compare against the current one, or writing any command that stashes.

## An audit round's own fixes are the next round's findings, and test strength is where they land
- Type: recurring-finding
- Observed: US-013 phase 3 — three audit rounds over one branch
- Learning: three rounds ran 16, then 7, then 9 findings, and the shape shifted rather than shrank. Round one was mostly production defects (a formatter emitting a clause twice, a viewer label swallowing an arrow's pointer events); rounds two and three were almost entirely about the *tests written to fix round one* — a bounding-box assertion that passed on a stale placement, a `require.NotEqual` shadowed by the `require.Equal` above it, an x-bound spanning most of the canvas, three call sites where only one was pinned. Each was a guard written in a hurry to close a finding, and each was itself unfalsifiable. The lesson is not "run more rounds" but "a fix that adds a guard owes the same mutation proof the original code did": run the mutation the finding described *and* ask what else could make the new assertion pass. Note also that a narrowed re-audit is nearly free when the raising lenses cover most of the panel anyway, and that no finding from an earlier round was ever re-raised — `recheck` did its job, so the growth was genuinely new surface, not regression.
- Apply when: fixing audit findings, or deciding whether a second or third audit round is worth its cost.

## A distance-to-path check cannot see a label placed at the wrong point *along* the path
- Type: recurring-finding
- Observed: US-013 phase 3 — guarding where the viewer draws an arrow's text
- Learning: two different defects hide behind "the label is on the arrow", and one assertion sees only one of them. `Layout.edgeLabelPoint` interpolating an arrow's endpoints while `computeArrowD` draws a cubic Bézier puts the label *off* the line — a perpendicular error, caught by measuring distance to the emitted path (measured 3.6px for sibling slices, 8.2px across contexts; an audit's "~25px" was an overstatement worth checking rather than repeating). A drag that reshapes the arrow without moving the label puts it at the wrong point *along* the line — a parameter error, which a distance check often cannot see at all, and which a bounding-box check never can. The straight-segment case is the trap: a stale point frequently still lies within a pixel of the moved line. Guard the two separately — distance to the path for placement, and for tracking either an exact expected point or a gesture whose delta is perpendicular to the arrow. The derived fix is to make one function own the route (`arrowCurve`) and have the renderer and the sampler both read it, so the two can never disagree about a curve again.
- Apply when: asserting where a diagram or viewer paints text relative to a line, or reviewing a geometry assertion phrased as "is on the arrow".

## A positional TextMate rule is only witnessed by a construct that reaches its operand
- Type: recurring-finding
- Observed: US-013 task 1 — colouring `after` case-sensitively
- Learning: `#positional-keywords` keys each entry on its operand (`\bafter(?=\s+")`), so an assertion meant to prove the rule is case-sensitive only proves it if the construct under test actually reaches that lookahead *and* no earlier rule has already consumed it. `automation After { every "5m" ... }` fails both ways — `#keyword-entity` matches `automation After` whole before `#positional-keywords` is consulted, and the brace after `After` is not a quoted string — so making the rule `(?i)` left the whole scope suite green. `invariant After "A hold expires once"` is the witness: `invariant` is in the flat alternation, so the name is left for the positional rule, and its quoted statement is exactly the operand. Pair any such fixture with a mutation guard that makes the positional rules case-insensitive and requires the assertion to fail, which is the only thing that keeps the claim honest.
- Apply when: adding a keyword to `editors/vscode/syntaxes/emod.tmLanguage.json`, or asserting that a scope rule is case-sensitive.

## Look at the rendered page: it finds what a green viewer suite cannot
- Type: convention
- Observed: US-013 task 10 — drawing an automation's delay in the viewer
- Learning: the delay label's first placement — the midpoint between the two box centres — passed all 384 viewer tests and landed squarely on top of a command's box in the browser, because an activation arrow climbs past the command row on its way from the event to the automation. jsdom asserts structure and geometry, never overlap or legibility, so nothing in the suite could have said so. Building the bundle (`task build:web`), serving it and looking took about two minutes and produced a real fix plus a named constant for the offset. The reverse also holds: the fix then needed a test, because looking is not a regression guard. Note the viewer's detail panel does not open from a synthetic click in an automated browser session; drive `UI.showDetailPanel` through a dynamic `import()` in the page instead of chasing the interaction.
- Apply when: adding anything the viewer draws, especially text placed relative to something else.

## `emod fmt` renders each activation line independently, so a two-activation automation doubles a shared suffix
- Type: recurring-finding
- Observed: US-013 task 4 — writing the delay as a suffix
- Learning: `writeAutomation` emits the `on` line and the `every` line through separate `lineIfSet` calls, so a suffix that belongs to "whichever activation the automation states" is written by both when it states both — text no author wrote. The parser rejects that model, which is why every CLI path is gated, but `internal/wasm`'s `ExportEmod` (diagram JSON → `importer.ImportDiagram` → `formatter.Format`) is not, and the importer copies `on_event`, `every` and `after` with no cross-check. A suffix shared between two mutually exclusive lines therefore needs an explicit "not if the other one is stated" guard, and the golden that catches it is an AST built by hand, since the shape cannot be parsed from source.
- Apply when: adding a formatter suffix or modifier that could attach to more than one line of the same block.

## A breakdown's blast-radius count goes stale when sibling stories land after it
- Type: constraint
- Observed: task 1 — US-014 format the new constructs consistently
- Learning: US-014's decomposition measured its own churn by walking every checked-in model, and every count it recorded was wrong by delivery time because US-007, US-009 and US-010 landed in between and each added canonical constants of its own: the reorder moved 7 view transcriptions where the walk said 3, the flow alignment moved 4 expected values where the criterion said 'moves no existing expected value', and payloadFormattedEmod wrapped where the criterion assumed no payload could. The rule each criterion stated held exactly — only values transcribing the changed shape moved — so the fix is to re-measure and amend the criterion to name the measured set, never to tick a false count and never to bend the code toward it. A criterion phrased as a rule ('only values transcribing X move') survives the drift; one phrased as a count ('exactly these three') does not.
- Apply when: writing a blast-radius measurement into a breakdown for a story that will land after its siblings, or closing a change-set criterion whose numbers came from decomposition time

## A budget with several measured shapes needs one boundary leaf per shape
- Type: recurring-finding
- Observed: task 4 — US-014 format the new constructs consistently
- Learning: US-014's payload budget was pinned exactly-at and one-past for a single shape — a spec stating nothing but 'when', whose keyword takes no padding and which sits in no list — and the criterion read 'both boundaries asserted, so the comparison cannot be off by one'. It could: three off-by-one measurement errors each left the entire unit suite green while changing real 'emod fmt' output, because the formatter measures three different texts. Dropping the keyword column's padding from the measured head, dropping a list element's trailing separator, and measuring an element one indent level too deep were all invisible. Count the shapes the comparison actually measures — what the head contributes, what the separator contributes, which indent level applies — and give each its own exactly-at/one-past pair; asserting the fixture's own length before asserting it survives is what keeps the arithmetic honest.
- Apply when: writing or reviewing the boundary tests for any width, length or size threshold whose measured text is assembled from more than one part

## A tree-sitter corpus case cannot discriminate layout, but can discriminate token adjacency
- Type: constraint
- Observed: task 4 — US-014 format the new constructs consistently
- Learning: 'editors/tree-sitter-emod/grammar.js' declares extras: [/\s/, $.comment], so newlines, indentation and column padding are lexically invisible: a corpus case named for a wrapped or aligned shape cannot fail for that shape, and collapsing its source onto one line leaves it green. US-014 wrote one, renamed it to claim layout, and only a second audit round caught that the name outran the case. What a case still discriminates is which tokens may sit next to which: a third round proved the wrapped case is the only one of 93 that dies when a list comma is required to be followed by same-line whitespace, because it is the only spec_event_list in the corpus broken across lines. So name such a case for the token sequence it pins, and note that nothing holds a case claiming to be formatter output to that claim — no Go test reads the corpus and re-formats it, so the source is a snapshot that drifts silently when the formatter's shape moves.
- Apply when: adding a tree-sitter corpus case for output whose shape is a matter of layout, or reviewing one whose name claims a wrapped, aligned or broken form

## A context-level invariant is only reachable from a DCB context's own slices
- Type: constraint
- Observed: task 5 — US-014 format the new constructs consistently
- Learning: A rejection — a 'then rejected X' spec or a 'command -> rejected:' flow entry — inside an aggregate slice must name an invariant that aggregate declares; the validator reports 'invariant X is not declared in aggregate Y' otherwise. So an invariant declared on a context that holds its slices through aggregates can be referenced by nothing, and 'spec/invariant-never-exercised' fires on it every time. Context-level invariants are usable only where slices hang off the context directly, which is 'mode dcb'. This is why a fixture meant to state an invariant in both homes must put the context-level one on the DCB half, and why a criterion asking for 'an invariant on a context' is unsatisfiable unless it says which kind of context.
- Apply when: writing a fixture or example that declares an invariant on a context, or a criterion asking for one

## Fixing an over-claiming test name by naming the delivery batch trades one finding for another
- Type: recurring-finding
- Observed: task 5 — US-014 format the new constructs consistently
- Learning: An audit flagged a fixture called 'every construct the language has' for omitting an 'optional' modifier, an aggregate- or mixed-mode context and an or/not predicate. Narrowing three test names to 'every construct this batch adds' fixed the over-claim and immediately drew a fresh finding from a different lens: it names code by its plan position, which a reader of a failure cannot resolve and which stops referring to anything once the next batch lands. The only wording that satisfies both is the one that states the constructs — 'wire types, delays, rejections, spec payloads and every spec outcome' — and a fixture doc that closes by naming what it deliberately leaves to other fixtures, the way its neighbours already do. Reach for the construct list first; an increment name and a completeness claim are two ways of not saying what is covered.
- Apply when: naming a shared fixture or the tests that consume it, or fixing a name an audit called an over-claim

## The formatter measures a line in runes and pads a column in bytes
- Type: constraint
- Observed: — US-014 format the new constructs consistently
- Learning: 'fitsOnOneLine' (internal/formatter/formatter.go) counts runes, because maxLineWidth is a column budget and a payload value holding an accented letter or an em dash would otherwise be charged two or three columns for one — the shipped first version used len() and wrapped a 100-column line at 100 bytes. 'columnWidth', which drives every '%-*s' pad in the file (field names and types, tag keys, flow-block prefixes, spec keywords, payload labels), still counts bytes. The two disagree only for a non-ASCII cell, and no cell can be one today: internal/lexer/tokenizer.go admits only ASCII identifiers, and every padded column is an identifier or a keyword. The divergence is therefore unreachable rather than fixed, and it is unreachable for a reason that lives in the lexer — widen the lexer to accept a non-ASCII identifier and every aligned column in the formatter misaligns at once, with no test able to catch it beforehand.
- Apply when: changing what internal/lexer accepts as an identifier, or adding a column to the formatter that could hold text rather than an identifier

## --in-place on the default branch leaves no branch to review or to integrate
- Type: constraint
- Observed: — US-014 format the new constructs consistently
- Learning: The implement method says that with in_place on you create a feature branch if you are on the default branch and build in the main checkout. Running US-014 with '--in-place --integrate' from main, the branch step was skipped and all nine commits landed straight on main, so 'clerk land --integrate' answered 'already on main — nothing to integrate' and there was never a reviewable branch. The end state matches what the correct process would have produced only because integrate was also on; with '--no-integrate' the same miss would have put unreviewed work permanently on the default branch. It also means both audit rounds and their aborted fixup replays ran against main rather than a branch. Create the branch first — the flag turns off the worktree, not the branch.
- Apply when: starting an implement run with --in-place while the checkout is on the default branch

## A quoted name is where non-ASCII actually reaches the toolchain
- Type: constraint
- Observed: US-015 navigate and complete the new constructs in the editor
- Learning: The recorded formatter learning calls the rune/byte divergence unreachable because `internal/lexer` admits only ASCII identifiers and every padded column holds an identifier or keyword. That reasoning does not carry to any surface handling *quoted* names: a model, actor, context, aggregate, slice, trigger or spec name is a string literal holding arbitrary prose, so `context "Réservations"` is 12 characters in 13 bytes today, with no lexer change needed. `internal/lsp/position.go` measures a name with `nameWidth` (utf8.RuneCountInString) for exactly this reason — measured before the fix, hover on that context reported the range 9..22 for a name occupying 9..20 and resolved with the cursor on the closing quote. The *origin* is still byte-based and unfixed: `advance` (`internal/lexer/tokenizer.go`) does `c.column++` per byte, so any position sitting after non-ASCII on the same line is off, and LSP's default encoding is UTF-16 code units rather than runes in any case. Fixing a width and believing positions are now correct is the trap.
- Apply when: measuring or emitting a position, column or width for a construct whose name is quoted, or judging whether the lexer's ASCII-identifier rule makes a byte/rune divergence unreachable

## State the completer's line scanner carries across lines must be bounded by the block that opened it
- Type: constraint
- Observed: US-015 navigate and complete the new constructs in the editor
- Learning: `blockScanner` (`internal/lsp/completer.go`) never parses, so any state it holds between lines outlives whatever the author has half-typed. `emod fmt` wraps a spec entry past the column budget — `given [` on one line, each element on its own below — which forced the entry keyword to survive the line that stated it; the first attempt re-derived it from each line's own bracket count and lost every element after the first, and the second held it while a bracket depth accumulated across lines with nothing resetting it. A single unclosed `[` then armed the entry to end of file: every later `{` opened a payload block instead of its real block and `GetCompletions` returned an empty list for the rest of the document, including sibling contexts and the top level — a regression against the base, found only by the audit round after the one that introduced it. The fix records `len(s.blocks)` when the list opens and clears both depth and entry once the stack drops below it. Any future cross-line field needs the same bound, and its guard is a document left deliberately unclosed with assertions on a *later sibling* block, not on the construct that owns the state.
- Apply when: adding a field to blockScanner that survives a call to consume, or reviewing a completer change that carries a keyword, bracket or brace state between lines

## An enumeration in a task silently narrows the story, and no lens audits the task file
- Type: recurring-finding
- Observed: US-015 navigate and complete the new constructs in the editor
- Learning: US-015's story criterion reads "Hovering any construct shows its kind and description"; the breakdown's Task 1 turned that into eight named constructs and omitted `spec` — in the story about specs. A spec's quoted name returned nil while the slice and invariant beside it answered. Three adversarial audit rounds missed it, and their own `coverage_gaps` said why: every lens owns changed source files, the task .md and .json are owned by none, and each lens judges intent from the diff and the brief rather than from the story. The catch came from re-reading `user-stories/*.md` against the branch, which is the one step that reads the request rather than a restatement of it. Treat any task criterion that *lists* what a story states as a category ('every construct', 'each format', 'all four patterns') as a narrowing to re-derive from the source, and expect the audit to confirm the list rather than question it.
- Apply when: decomposing a story whose criterion names a category rather than members, or validating a finished branch against the story after an audit reports clean

## Group a clerk fixup by what exists at the target commit, not by which file the fix is in
- Type: convention
- Observed: US-015 navigate and complete the new constructs in the editor
- Learning: `clerk fixup` stages whole files and records a diff against the branch tip, and `--replay` applies that diff at the target commit — so a hunk whose surrounding lines were introduced by a *later* commit cannot apply, and the whole replay aborts. Two conflicts cost a rebuild each on one branch. The first was a test file carrying fixes for two tasks: a leaf added by task 8 was staged into task 7's fixup, and task 7's tree has no such leaf. The second was a new subtest anchored beside one a later task added. The rule is to group by the earliest commit at which every line the hunk touches already exists, which for a file several tasks edited means splitting the work into rounds — revert the later-owned hunks, fixup the early target, restore them, fixup the late one. Where a fix is genuinely new work rather than a correction, `clerk fixup` says so itself and refuses (`no commit in <range> touches <file>`), which is the cue to give it its own commit instead of forcing a fold.
- Apply when: folding audit findings into the commits that introduced them, especially when one test file carries fixes belonging to more than one task

## An empty line in a multi-line SVG or draw.io label does not exist
- Type: constraint
- Observed: task 1 — US-016 render specs on diagrams
- Learning: A `<tspan>` with no character data carries no glyph, so its `dy` never advances the text position and the shift does not carry to the next tspan — the blank line simply is not drawn, and the box measured for it is that much too tall. Measured in Chrome against `svgMultilineText` (`internal/diagram/svg.go`): a 12-line card with two blank separators rendered its ink 11px from the top and 40px from the bottom of a box sized for 12 lines, with the scenarios running together. draw.io's `whiteSpace=wrap` value behaves the same way. Separate blocks inside one label with a mark that carries ink — the spec card quotes each scenario's name, which is also how the DSL writes it — and never with an empty string.
- Apply when: Building a multi-line label for internal/diagram's SVG or draw.io writer, or sizing a box from a line count that includes blank lines

## Looking at one rendered fixture proves nothing about text the fixture does not contain
- Type: recurring-finding
- Observed: task 1 — US-016 render specs on diagrams
- Learning: Rendering `test.SlicePatternLibraryLending` in a browser and measuring every card showed 13px of horizontal clearance, which read as "no overflow". It was a fact about that fixture: its longest scenario name is 47 characters. An audit built a 92-character name and rasterised it — 416px of text in a 260px card, clipped at both viewBox edges. The same measurement then showed the rune budget chosen from prose (~4.6px/rune) was wrong for identifier lines, which run to ~5.7px/rune, so `then [SomeCamelCaseEvent, AnotherOne]` overflows while staying under budget. When the input is arbitrary author text, look at the extreme the fixture does not contain and calibrate any width budget against the widest glyph mix, not the average — and note that `font-family="sans-serif"` resolves to a wider face than Helvetica on Linux, so the margin is smaller than a macOS measurement suggests.
- Apply when: Verifying a rendering by eye, or choosing a character budget that stands in for a text measurement

## A per-slice shape needs its column asserted, not just its label and its count
- Type: recurring-finding
- Observed: task 2 — US-016 render specs on diagrams
- Learning: "Draws one card per spec-stating slice" plus "the cards name every scenario in declaration order" leaves the binding between the two unasserted: swapping two cards' x inside `specCards` left every Go test in the repository green, so the story's headline criterion ("a card under the slice") would have survived cards drawn under the wrong slices. A card carries no slice name, so the only thing tying it to one is sharing a column with a construct that slice declares — assert that, by taking the centre x of a box the slice owns and requiring the card's x-span to contain it. The rejection badges added by US-009 already do exactly this and say why in their own message (`internal/diagram/svg_test.go`, "each slice's dashed arrow ends at the badge in its own column"); note the lookup must exclude the cards themselves, since `boxLabelled` matches on substring and a card's text names the very constructs its slice declares.
- Apply when: Adding a per-slice shape to the SVG or draw.io exporter, or writing the receipt that it belongs to the slice it was drawn for

## Never restore a mutation with git checkout while the feature is uncommitted
- Type: constraint
- Observed: US-016 render specs on diagrams
- Learning: A mutation-test harness that ends each case with `git checkout -- <dir>` restores tracked files to HEAD — which, before the feature is committed, is the feature itself. One such loop erased a finished task's four tracked files in a second, leaving only the new untracked file behind; the surviving cases then reported "ok ... [no tests to run]", which reads like a caught mutation rather than a wiped tree. Mutate against a copy: back the files up to the scratchpad and restore with `/bin/cp -f`, or commit first and mutate afterwards. The same run also hit the recorded prezto trap from the other side — `rm` is aliased to `rm -i`, so deleting a scratch test file hung until the tool timeout.
- Apply when: Writing a loop that mutates production code to prove a test can fail, or reaching for git checkout to undo a temporary edit

## clerk verify reports every Test function as dead code
- Type: constraint
- Observed: task 3 — US-016 render specs on diagrams
- Learning: The dead-code check reports "defined but referenced nowhere outside its own file and the tests", which is the defining property of a `Test…` function: `go test` discovers it by name and nothing ever references it. `TestExporterSpecCards` (`internal/diagram/contract_test.go`) blocked the gate while running 12 leaves across both picture exporters. This widens the recorded `internal/test` false positive from fixture symbols to any new top-level test function. Confirm with `go test -run '<Name>' -v` that it executes, record the judgement, and pass `--audit-accepted` to `clerk land`; do not delete or rename a live test to open the gate.
- Apply when: clerk verify blocks on dead code for a symbol whose name starts with Test, or for anything in internal/test

## A variadic option added to an exporter moves no call but breaks every function-value assignment
- Type: convention
- Observed: task 1 — US-016 render specs on diagrams
- Learning: Giving `ExportSVG`/`ExportDrawio` a trailing `...Option` left all 111 call sites in `internal/diagram` and the four in `internal/cli` compiling unedited — which is the whole reason to prefer it over a positional parameter or a widened struct. What it does break is assignment: Go will not accept `func(*ast.Model, Style, ...Option)` where `func(*ast.Model, Style)` is declared, so `contract_test.go`'s `exporter.export` field needs a non-variadic wrapper (`exportSVGDefault`). Keep the shared field non-variadic and add a second `exportWithSpecs` field left nil for the text formats, rather than making the field variadic: a variadic field would let the harness hand mermaid and ASCII an option they cannot honour, which is the silent-ignore trap this repo already records against `Style`. A criterion phrased "every existing call compiles unedited" is satisfiable; "the diff in that file is additions only" is not.
- Apply when: Adding a functional option to an existing exported function, or writing the change-set criterion for one

## A TextMate block rule with an optional brace also opens on a field named after its keyword
- Type: constraint
- Observed: task 4 — US-017 highlight the new syntax in editors
- Learning: `#tags-block`'s begin was written `(?i)\\b(tags)\\b\\s*(\\{)?`, copied from `#fields-block`. The optional brace made it fire on a spec payload field *named* `tags`, and because the rule deliberately omits `#payload-values`, every literal from there to the payload's closing brace lost its scope — `when Claim { tags: 1, amount: 42, ok: true }` rendered all three unscoped, with the whole suite green. The optional brace is load-bearing in `#fields-block` (a block may put its brace on the next line) and harmless there only because that rule keeps the literal patterns. Any block keyword in emod is also a legal field and payload-field name, so a begin rule keyed on one must require its brace unless it keeps everything the enclosing context had.
- Apply when: Adding a begin/end rule to editors/vscode/syntaxes/emod.tmLanguage.json keyed on a DSL keyword, or reviewing one whose begin makes the brace optional

## Claiming a position in TextMate retires the mutation controls that witnessed there
- Type: recurring-finding
- Observed: task 4 — US-017 highlight the new syntax in editors
- Learning: A rule that claims a token by position shields it from every later rule, which is the point — and which silently voids any negative control whose witness sat at that position. Adding `#field-name` moved all three flat-alternation controls off `on string required` onto `notified on required`, one column along; their titles still said 'a field named after one' and were then false rather than stale. It also left `unreserved-keywords.emod`'s fifteen bare-prohibition assertions satisfied by construction. After adding a positional rule, re-run each existing control and check it still fails on the line its `sourceLine` names, and give the newly-shielded assertions the positive scope so a mutation can still reach them.
- Apply when: Adding a positional or block-scoped rule to the TextMate grammar, or finding that a negative control still passes after a grammar change

## Editor-fixture marker columns must be generated, never counted
- Type: recurring-finding
- Observed: task 1 — US-017 highlight the new syntax in editors
- Learning: Both assertion formats locate their claim by column — tree-sitter's `#   ^ capture` and vscode-tmgrammar-test's `#   ^^^^ scope` — and a marker one column off asserts against a different token, or against nothing, and passes forever. Three occurred in one story: a `- constant.numeric.emod` marker at column 6 of `emod 1` (the digit is at 5, so it asserted past end-of-line), a caret covering `oomId:` instead of `roomId`, and two payload literal markers. Every one was hand-counted; every generated one was correct. Derive the column from `line.index(token)` and sweep afterwards, asserting each caret run covers exactly one whole token — the check is ten lines and found two defects the suites and two audit rounds' worth of lenses did not.
- Apply when: Writing or reviewing an assertion in editors/tree-sitter-emod/test/highlight/*.emod or editors/vscode/test/scopes/*.emod

## A scope prohibition needs the positive scope beside it or it is satisfied by construction
- Type: convention
- Observed: task 2 — US-017 highlight the new syntax in editors
- Learning: `- some.scope` passes whenever nothing could have produced that scope, so a prohibition on a token no rule can reach is unfalsifiable — it survives every mutation and reads as coverage. Two shipped this way in one story: the tag key `entity` prohibited a boolean scope it matches no spelling of, and fifteen field-name prohibitions in `unreserved-keywords.emod` became vacuous the moment `#field-name` guaranteed them. The fix is the same in both cases and costs one token: state the scope the position actually carries alongside the prohibition, so a mutation that changes the treatment reddens the line.
- Apply when: Writing a `- scope` assertion in editors/vscode/test/scopes/*.emod, or reviewing one whose token no rule in the grammar could match

## A mismatch helper must tie its fragments to one reported failure
- Type: convention
- Observed: task 4 — US-017 highlight the new syntax in editors
- Learning: `assertReportsMismatch` in `editors/vscode/test/scope-assertions.test.js` checked its sourceLine, required, prohibited and produced fragments with independent `output.includes` over the whole harness report. A mutation that reddens several lines — the field-name one reddens eleven — therefore lets four fragments come from four different failures, so a control passes while the line it names carries none of what it claims. Demonstrated by dropping the field-name requirement from the exact line the field-name control names: green before, red after. Slice the report into its `at [file:line:col:col]` blocks, require exactly one to quote sourceLine, and check the rest against that block only.
- Apply when: Writing or reviewing a helper that asserts an external tool's failure report names an expected position and scope

## A criterion requiring existing guards to pass unedited is unsatisfiable when the repair is what they witnessed
- Type: recurring-finding
- Observed: US-017 highlight the new syntax in editors
- Learning: US-017's Task 2 carried 'the four negative controls still pass, the on/every one unedited — extending the flat alternation must still break the field-name assertions'. Claiming a field's name is precisely what stops a positionless alternation reaching that position, so the task's own repair voided the witness the criterion demanded be preserved; satisfying it would have required the per-keyword allowlist the same task's first decision ruled out. The criterion also said 'four' where sibling stories had since made it five. A criterion that pins the *count* or the *exact wording* of guards a repair is about to change is asserting the repair does not work — pin what the guards must still catch instead, and let their witness lines move.
- Apply when: Writing an acceptance criterion that requires existing tests to pass unedited, or one that counts the tests in a file the task will change

## A decision that declines a repair by naming its cost owes that cost a measurement
- Type: recurring-finding
- Observed: US-017 highlight the new syntax in editors
- Learning: US-017's decision 3 declined to repair the single-line `fields { name type modifier }` form, reasoning that TextMate 'has no anchor for the first token after the block's opening brace short of folding it into the begin pattern's captures', and required an assertion recording the defect instead. The fold cost one optional group and one capture, all twelve fixtures stayed green, and the story's criterion carried no layout qualifier — so a criterion was closed on a recorded defect that a one-line change removed. When a decision names the alternative it is rejecting, the alternative is usually cheap enough to try before the breakdown is written.
- Apply when: Writing a decomposition decision that declines a repair on cost grounds, or implementing a task whose criterion records a defect rather than fixing it

## A directory-enumerating guard asserts nothing about a file being there
- Type: constraint
- Observed: task 3 — US-018 learn the new constructs from examples and the reference
- Learning: `examplePaths` (`internal/cli/validate_test.go`) reads `../../examples` and runs every non-`_test.emod` file through `cli.RunValidate`, so it asserts things about whatever files exist and never that a named one does. Deleting `examples/specs_hotel.emod` outright left the whole unit suite green, because `require.NotEmpty(t, authoredToValidate)` is satisfied by the siblings. Worse, `emod validate` exits 0 on a zero-byte and on a whitespace-only `.emod` (verified), so gutting an example to nothing also passes. A story criterion naming a specific example therefore needs a leaf that reads that file by name and asserts what it states; the enumerating guard is coverage for the directory, not for the artefact.
- Apply when: citing `examplePaths` as the guard for a new file under `examples/`, or writing a criterion that says an existing directory-walking test covers a named artefact

## A plan's claim about what a tool would do is an estimate until someone runs it
- Type: recurring-finding
- Observed: task 4 — US-018 learn the new constructs from examples and the reference
- Learning: US-018's Open question 5 ruled out formatting the checked-in examples because `emod fmt` "would insert the header, re-align every field column, hoist invariants, move every spec to the end of its slice and drop the file's blank lines, producing a diff that buries the story's change". Measured by copying the file to a scratch path and running `emod fmt` over the copy, the whole distance was three lines — the formatter hoists a view's `subscribes` above its `fields`, and nothing else. No header was inserted (one was already there), no field column moved, no flow was reordered. The estimate was the entire basis for the decision, and it was wrong in the direction that avoided work. A decomposition that rules something out by describing a tool's output owes one run of that tool against a copy; `emod fmt <file>` writes in place, so the copy is what makes the measurement free.
- Apply when: writing or reading a decomposition that declines a formatting, codegen or migration pass by describing how large the resulting diff would be

## A mutation written against pre-format text proves nothing and looks like a passing guard
- Type: constraint
- Observed: task 2 — US-018 learn the new constructs from examples and the reference
- Learning: After the examples were brought to `emod fmt` canonical form, a mutation loop kept matching the hand-written text: `then view AvailableRoomsView` where the formatter writes `then  view AvailableRoomsView` with two spaces, since it pads the given/when/then keyword column. The `perl -0pi -e` substitution silently matched nothing, the leaf passed, and the run read exactly like a guard that cannot fail — the failure mode a mutation proof exists to detect. Same trap for `command -> event:` (fmt pads to `command -> event:    `). Check the mutation actually changed the file (`grep -c` the old text, expect 0) before reading a green result as evidence, and write the pattern against the file as it is on disk, never against the form it was authored in.
- Apply when: writing a mutation loop over a file a formatter has rewritten, or reading a mutation run where nothing failed

## A loop-closure guard checks one edge, not that a todo list ever empties
- Type: recurring-finding
- Observed: task 2 — US-018 learn the new constructs from examples and the reference
- Learning: `todoListLoops` (`internal/cli/examples_test.go`) checks that an automation reading a view issues a command producing an event that view subscribes to — the property the specs-and-metadata proposal states as the point of its worked example. It passed on `UnconfirmedReservationsView` while that view kept a row forever in two ways: a confirmed booking, because the view never observed `ConfirmationEmailSent`, and an ended stay, because the nightly sweep's command is refused by an invariant and a rejection appends nothing to observe. Two separate audit rounds found todo-list defects this guard was green on. Which events *retire* a row is a domain judgement; the mechanical check only sees the one edge the automation itself creates, so do not read it as a completeness check, and read every `subscribes` list against the view's own name when a model gains an event.
- Apply when: adding a view or an event to a model that declares automations, or judging whether a loop-closure assertion covers a todo list emptying

## A hand-written skeleton in the reference can disagree with emod fmt, and nothing checks it
- Type: convention
- Observed: task 4 — US-018 learn the new constructs from examples and the reference
- Learning: `docs/dsl-reference.md`'s View Pattern skeleton wrote `fields { ... }` above `subscribes [...]` while `emod fmt` canonicalises `subscribes` first, so a reader copying the skeleton wrote what the formatter immediately rewrites. Only whole models behind an ```emod fence reach `internal/oracle`'s "documented models" leaf; every skeleton with `<placeholder>` names sits behind a plain fence and is checked by nothing, which is why the disagreement survived. When a change makes canonical form a deliverable for the examples the reference points at, the reference's own skeletons become part of that claim: check each one against what the formatter writes for the same construct. Verified by listing every `view … {` block in the document and comparing its entry order against `formatter.Format`.
- Apply when: adding or editing a plain-fenced skeleton in `docs/dsl-reference.md`, or making `emod fmt` canonical form a requirement anywhere

## A go list pipeline is emptied by the very package it filters
- Type: constraint
- Observed: task 8 — US-001 render a model in a native desktop window
- Learning: `go list ./...` exits 1 and prints **nothing** when any package fails to load, so `go test $(go list ./... | grep -v /broken)` runs with no package arguments and falls back to the repo root — `FAIL . [setup failed] no Go files`. `task test:unit` and `test:integration` use exactly that shape to exclude `cmd/emod-wasm`, and adding `cmd/emod-desktop` (whose `//go:embed all:frontend` cannot resolve until `task build:desktop` has assembled it) broke both on every checkout where that directory is absent — which is CI, and any fresh clone. The fix is `go list -e ./...`, which reports the broken package and exits 0 so the filter has something to filter. The trap is that it is invisible to whoever adds the exclusion: they have just run the build, so the directory exists locally and the suite is green. Verify an exclusion by deleting what makes the package unbuildable and running the suite, not by confirming the package would fail without it.
- Apply when: adding a package to the grep -v exclusion in Taskfile.yml's test:unit or test:integration, or adding any package whose //go:embed target is gitignored

## Two concurrent dynamic imports race the mock; one flush between them does not
- Type: recurring-finding
- Observed: US-001 render a model in a native desktop window
- Learning: A vitest `vi.mock` of a module reached through a dynamic `import()` binds reliably only when one import has settled before the next starts. `Model.sendParse` reaches the seam with `import('./platform.js')`, so two `render-btn` clicks fired back-to-back start two concurrent imports and the second resolves to the **real** module — the symptom is a status of '✗ WASM initialization failed: Go is not defined' and a mock call count one lower than the click count, in an isolated test file, and no number of trailing `await flush()` calls fixes it. One `await flush()` **between** the clicks fixes it completely. This cost a guard shipping untested and a commit message claiming a test was impossible; an audit round then wrote the test in one attempt. When a test looks impossible because of module mechanics, vary *where* the turn is taken, not how many.
- Apply when: writing a vitest test that drives two overlapping async operations through a module the suite mocks, especially one the code under test reaches by dynamic import

## A Wails build without -tags production keeps a live dev-server proxy path
- Type: constraint
- Observed: task 9 — US-001 render a model in a native desktop window
- Learning: `go build` of a Wails v3 app without `-tags production` compiles in `internal/assetserver`'s dev build: `GetDevServerURL()` reads `FRONTEND_DEVSERVER_URL` and the asset server proxies to whatever it names, and the bundled webview runtime is the 516 KB `runtime.debug.js` with devtools rather than the 57 KB production one. With the tag, `GetDevServerURL()` returns "" unconditionally and the path is compiled out — measured on this repo's binary, three references to that variable become none and it shrinks from 16.1 MB to 13.5 MB. `/wails/runtime.js` is served from a compiled-in constant either way, so bindings resolve in both. A criterion like 'renders with no network access' is otherwise satisfied only by the environment variable happening to be unset. Note the tag also silences the asset server's per-request logging, which is the easiest evidence that a window loaded its modules — capture that from a debug build before switching.
- Apply when: adding or reviewing a Wails build task, or judging whether a desktop binary honours a no-network criterion

## A python index-slice edit corrupts a doc when the boundary string also appears above
- Type: recurring-finding
- Observed: US-001 render a model in a native desktop window
- Learning: Ticking a task's checkboxes with `s.index(start_marker)` / `s.index(end_marker)` silently duplicates a region when the end marker also occurs *before* the start — `s[start:end]` is empty, nothing is ticked, and `s[:start] + blk + s[end:]` re-emits everything between them. Using a '**Phase N —' boundary did exactly that, because that string is also a row in the file's Contents table: the US-001 breakdown went 1009 → 1361 → 2325 lines across two commits, carrying four copies of its own `## Tasks` section, while the tasks it was meant to tick stayed unticked. `clerk lint --staged` passed every time — nothing checks a breakdown for structural damage. Assert `start < end` and that the slice starts with the heading you meant, bound on the unique `### Task N:` headings rather than prose, and diff the result before staging: line count unchanged and only checkbox lines moved.
- Apply when: editing a markdown breakdown or any structured document by string-index slicing rather than a parser

## A Go symbol consumed only by generated JS bindings reads as dead code
- Type: convention
- Observed: US-001 render a model in a native desktop window
- Learning: `clerk verify`'s dead-code check reports a method 'referenced nowhere outside its own file and the tests' for any exported symbol whose only real consumer is on the other side of a language boundary. `desktop.ModelService.ParseEmod` is called from `internal/frontend/desktop/platform.desktop.js` through bindings `wails3 generate bindings` emits, so no Go code names it. This widens the existing `internal/test` and `Test…` false positives to a third shape. Confirm with a grep for the JS caller and the generated binding, record the judgement, and pass `--audit-accepted` — and note the compiler will not catch a Go-side rename either once the tests are updated, which is why `internal/desktop/binding_names_test.go` reads the exported method names off both sides and requires them to agree.
- Apply when: clerk verify blocks on dead code for a Go symbol bound to a frontend, or adding a method to a type whose callers are generated bindings

## A jsdom assertion on text content cannot see that the text is off screen
- Type: recurring-finding
- Observed: US-001 render a model in a native desktop window
- Learning: `expect(statusEl.textContent).toContain(…)` passes on an element the user cannot see: jsdom has no layout, so a message written into a collapsed panel reads identically to one on screen. The viewer's export handler reported failures into `#render-status`, which sits inside the data panel that a successful render collapses, while the Export button sits outside it — so pressing Export after a render wrote the reason somewhere off screen, and a leaf asserting the text passed. It surfaced only when a human asked where the message was supposed to appear, and was measured by reading `getBoundingClientRect()` against `window.innerHeight` in a real browser. When a criterion is that a user is *told* something, a content assertion is a proxy; the thing itself needs either a layout-aware check or a look at the running page.
- Apply when: writing or reviewing a jsdom leaf that asserts a user-facing message, or closing a criterion phrased as the user being informed

## Wails adopts an application menu into a window only on UseApplicationMenu, and only Windows needs it
- Type: constraint
- Observed: US-002 open a model with a native file dialog
- Learning: WebviewWindowOptions.UseApplicationMenu defaults to false 'for backwards compatibility'. On macOS the application menu is always global, and on Linux webview_window_linux.go:338-341 falls back to globalApplication.applicationMenu whenever the window declares none — so both find the menu without the flag and the omission is invisible on a Mac. Windows is the exception: webview_window_windows.go:458 adopts it only when the flag is true, so a menu built with app.Menu.SetApplicationMenu simply does not exist there. Setting it changes nothing on macOS, confirmed by reading the installed menu back at runtime before and after.
- Apply when: adding or reviewing any menu in cmd/emod-desktop, or judging whether a desktop feature verified on macOS exists on the other platforms

## json.Marshal rewrites invalid UTF-8 as U+FFFD, so a Go service cannot promise bytes verbatim over JSON
- Type: recurring-finding
- Observed: US-002 open a model with a native file dialog
- Learning: encoding/json never errors on invalid UTF-8 — it substitutes U+FFFD per bad byte and reports success. Any Go service handing file contents to the frontend through a JSON envelope therefore alters a non-UTF-8 file silently, and no test notices because the round trip is self-consistent. desktop.FileService.Read refuses with 'reading <path>: not valid UTF-8' instead, which matters because US-003 writes the same string back to the path it came from: carried through, the first save on a Latin-1 model would destroy exactly the bytes that could not be read. Pair the refusal with a positive leaf on non-ASCII UTF-8, or the guard passes with the whole feature deleted.
- Apply when: putting file contents, or any caller-supplied bytes, through encoding/json on the way to a frontend

## text-overflow is inert on a flex container, and #stats span outranks a bare id selector
- Type: constraint
- Observed: US-002 open a model with a native file dialog
- Learning: Adding a truncating element to the viewer's status bar needs two things that are easy to miss together. text-overflow: ellipsis applies only to a block container, and '#stats span { display: inline-flex }' blockifies any span inside the bar to display:flex — so the rule is silently dead. Overriding it with '#stat-file-path { display: block }' also fails: (0,1,0,1) beats (0,1,0,0), so the type-qualified rule wins and the computed value stays flex. '#stats #stat-file-path' is what takes it. Four attempts against Chromium were needed to get an ellipsis to render, and every one of them left the jsdom suite green, because jsdom has no layout: read getComputedStyle().display and compare scrollWidth to clientWidth in a real browser rather than trusting the CSS you wrote.
- Apply when: adding or truncating an element inside #stats in internal/frontend/static/viewer.html, or overriding any rule that #stats span sets

## A resolves assertion must await the promise the code produced, not the helper's tail
- Type: recurring-finding
- Observed: US-002 open a model with a native file dialog
- Learning: A leaf shaped 'await expect(doIt()).resolves.toBeUndefined()' is vacuous whenever doIt ends in '.then(flush)' and the subscription it drives discards its own promise: flush resolves undefined unconditionally, and a throw inside the code under test is never observed. The fix is not more assertions but handing the promise back — 'Events.On(name, promptForFile)' instead of a wrapper that calls it and returns nothing — after which the same leaf fails on the exact defect it names. Two audit rounds disagreed about whether this leaf was vacuous; the deciding evidence was breaking the guard and re-running, not reading the code.
- Apply when: writing or reviewing a vitest leaf that asserts an async entry point does not throw, especially one reached through an event subscription

## A parity guard is vacuous until the far side of the boundary calls it, so it belongs to that task
- Type: convention
- Observed: US-002 open a model with a native file dialog
- Learning: A breakdown that lands a Go service in one task and its JS caller in a later one will put the cross-boundary name guard on the earlier task, where it cannot yet assert anything: the guard's shape is 'the JS calls are a non-empty subset of the Go methods', and with no JS caller either the NotEmpty floor fails or dropping it makes the guard pass with the feature absent. Plan the guard onto the task that introduces the caller, and expect the same shape for service registrations and wire keys — each is only assertable once both sides exist. Ticking it early is the failure to avoid; splitting one guard's edit across two commits is the other.
- Apply when: decomposing a story whose Go and frontend halves land in separate tasks, or deciding which task owns a guard that reads two files

## Audit rounds can contradict each other on a question the story never settled
- Type: convention
- Observed: US-002 open a model with a native file dialog
- Learning: One round asked for the open file's identity to be cleared when a paste replaces the model; the next called clearing it on any edit a defect. Both were 'confirmed' by reproduction, because each was right about its own scenario and the story decided neither. Taking each finding at face value oscillates the design. When two rounds pull opposite ways, the finding is really a product question: pick the reading the next story needs — here save-in-place, which requires the path to survive editing, giving text-editor semantics — state it in the Theory section and in the handover, and say which round's half you deliberately did not fix.
- Apply when: working audit findings across more than one round, or hitting a finding about behaviour the user story does not specify

## A low-certainty Wails shell task is routine once the framework API is read first
- Type: convention
- Observed: US-002 open a model with a native file dialog
- Learning: The File-menu task was planned low certainty on the grounds that cmd/emod-desktop had never built a menu or emitted an event, and it landed first try with a clean lint. What made it routine was reading menu.go, roles.go, menuitem.go and the dialogs source out of the module cache during Phase 0, before any task started — which also produced the accelerator syntax, the DefaultApplicationMenu composition and the cancel-as-empty-string contract. Budget the API read once at the start rather than pausing per task; the pause buys nothing the reading has not already answered.
- Apply when: assessing certainty for a task against an unfamiliar framework API, or deciding whether to pause on a low-certainty desktop-shell task
