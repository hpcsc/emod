version: '3'

tasks:
  test:
    desc: Run all tests
    cmds:
      - task: test:unit
      - task: test:integration

  test:unit:
    desc: Run unit tests
    cmds:
      - go test -tags unit ./...

  test:integration:
    desc: Run integration tests
    cmds:
      - go test -tags integration ./...
